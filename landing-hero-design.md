---
title: Glacier Landing Page Hero Design
tags:
  - Design
  - Landing-Page
  - Glacier
status: 待审阅
version: 0.1
related:
  - "[[design]]"
  - "[[趋势回调转强评分页面-PRD]]"
---

# Landing Page Hero 设计规范

## 1. 设计范围

本文件只定义网站首页首屏 Hero，不包含：

- 完整导航系统；
- 功能介绍、使用步骤、案例或定价区；
- 股票评分结果页；
- 登录、账户或历史记录页面；
- 生产代码与技术实现。

Hero 的唯一任务是让用户在数秒内理解产品用途，并立即输入一只A股代码开始评估。

## 2. 核心目标

用户进入页面后应立即理解：

1. 这是一个规则化的股票入场评估工具；
2. 它输出的不只是评分，还包括硬性过滤、止损和风险仓位；
3. 用户只需输入六位A股代码即可开始；
4. 结果用于辅助执行交易计划，不承诺收益。

Hero 的主转化目标：`提交股票代码并开始评估`。

次要目标：`查看评分规则`，帮助尚未准备输入代码的用户建立信任。

---

## 3. 视觉概念

### 3.1 主题

```text
Glacier Signal / 冰川信号
```

用冰层、等高线和精密仪表感表达“从复杂行情中提取清晰信号”。整体必须冷静、安静、克制，不使用交易软件常见的红绿闪烁，也不营造投机或收益承诺感。

### 3.2 视觉关键词

- 冰原留白；
- 深海蓝文字；
- 冰川蓝主操作；
- 轻微冷雾层次；
- 精确、可审计的数据卡；
- 稀疏的趋势线与网格；
- 未来感来自秩序与材质，而不是霓虹。

### 3.3 首屏气质

- 背景明亮但不刺眼；
- 视觉重心稳定，不用夸张倾斜构图；
- 主标题清晰、短促；
- 输入区域像一个精密工具的起点；
- 右侧视觉展示“结果结构”，不展示虚假的具体股票推荐。

---

## 4. Hero 文案

### 4.1 顶部标识

```text
趋势回调转强 · 日线交易模型
```

采用小型胶囊标签，帮助用户快速识别模型范围。

### 4.2 主标题

```text
在买入之前，
先把信号和风险算清楚。
```

主标题强调决策质量和风险意识，不强调预测准确率。

### 4.3 副标题

```text
输入一只A股代码，自动检查趋势、回调与转强信号，
再结合硬性过滤和止损风险，给出评分与计划仓位。
```

桌面端允许两行；移动端自然换行，不手动插入过多断点。

### 4.4 输入框文案

标签：

```text
股票代码
```

占位文字：

```text
输入六位A股代码，例如 600519
```

主按钮：

```text
开始评估
```

输入区域下方辅助文案：

```text
使用最新完整日线数据；盘中执行价格将单独确认。
```

### 4.5 次要行动

```text
查看评分规则
```

使用文字按钮或轻量描边按钮，不与主按钮竞争。

### 4.6 风险说明

```text
评分表示与模型的匹配程度，不代表上涨概率或投资建议。
```

放在Hero底部或输入区域下方，必须可见，但视觉权重低于辅助文案。

---

## 5. 桌面端布局

### 5.1 首屏结构

```text
┌────────────────────────────────────────────────────────────────────┐
│ Logo / 产品名                                      规则说明  关于 │
│                                                                    │
│  ┌────────────────────────────┐   ┌─────────────────────────────┐  │
│  │ 趋势回调转强 · 日线交易模型 │   │                             │  │
│  │                            │   │     评分结果预览资产          │  │
│  │ 在买入之前，                │   │                             │  │
│  │ 先把信号和风险算清楚。      │   │   82 / 95   +5 待确认       │  │
│  │                            │   │   硬性过滤：通过              │  │
│  │ 输入一只A股代码……          │   │   初始止损 / 风险仓位         │  │
│  │                            │   │   冰川趋势线背景              │  │
│  │ 股票代码                    │   │                             │  │
│  │ [ 600519           ][开始评估] │   └─────────────────────────────┘  │
│  │ 使用最新完整日线数据……      │                                    │
│  │ 查看评分规则                │                                    │
│  └────────────────────────────┘                                    │
│                                                                    │
│  评分表示与模型的匹配程度，不代表上涨概率或投资建议。               │
└────────────────────────────────────────────────────────────────────┘
```

### 5.2 网格

- Hero 内容最大宽度：`1280px`；
- 页面左右安全边距：`32px`，窄桌面为`24px`；
- 使用12列网格，列间距`24px`；
- 左侧内容占6列；
- 中间保留1列视觉呼吸区；
- 右侧视觉资产占5列；
- 左右内容垂直居中；
- Hero 内容区最小高度：`calc(100vh - 64px)`；
- 建议最小可视高度：`720px`，最大内容高度不强制锁定。

### 5.3 内容宽度

- 左侧文字最大宽度：`600px`；
- 主标题最大宽度：`570px`；
- 副标题最大宽度：`560px`；
- 输入组合最大宽度：`560px`；
- 右侧视觉资产建议宽度：`480–520px`；
- 右侧资产不得超过首屏视觉面积的42%。

### 5.4 垂直节奏

从上到下：

```text
模型标签
  20px
主标题
  24px
副标题
  32px
股票代码标签
  8px
输入与主CTA
  10px
数据辅助说明
  16px
次要CTA
  32px
风险说明
```

Hero 上下内容安全边距建议为`64–80px`。

---

## 6. Typography

沿用 `design.md` 的系统字体：

```text
Inter, "SF Pro Display", "PingFang SC", "Microsoft YaHei", sans-serif
```

数字使用等宽数字特性：

```text
font-variant-numeric: tabular-nums;
```

### 6.1 桌面端字号

| 元素 | 字号/行高 | 字重 | 颜色 |
|---|---|---:|---|
| 模型标签 | `13/18` | 550 | `glacier-700` |
| 主标题 | `52/62` | 650 | `text-primary` |
| 副标题 | `18/30` | 400 | `text-secondary` |
| 输入标签 | `13/18` | 550 | `text-primary` |
| 输入内容 | `16/24` | 500 | `text-primary` |
| 主CTA | `15/22` | 600 | 白色 |
| 次要CTA | `14/22` | 550 | `glacier-700` |
| 辅助文案 | `12/18` | 400 | `text-tertiary` |
| 风险说明 | `12/18` | 400 | `text-tertiary` |
| 视觉资产总分 | `40/44` | 650 | `text-primary` |

### 6.2 字体要求

- 主标题最多三行，推荐两行；
- 中文标题不使用全大写或过宽字间距；
- 主标题字间距建议`-0.02em`；
- 正文不使用300及以下字重；
- 评分预览中的数字必须稳定对齐；
- 不使用渐变文字。

---

## 7. 颜色与表面

### 7.1 Hero 背景

主背景：

```text
surface-page: #F8FBFD
```

背景可叠加两个极淡、不可交互的冷色光晕：

- 左上：`ice-cyan #69D2E7`，透明度不超过8%；
- 右侧：`polar-blue #568AF2`，透明度不超过6%；
- 光晕必须大范围模糊，不形成明显圆形色块；
- 不使用循环动画。

### 7.2 文字

- 主标题：`text-primary #102C3A`；
- 副标题：`text-secondary #476775`；
- 辅助和风险说明：`text-tertiary #718C98`；
- 链接和次要CTA：`glacier-700 #1E5A70`。

### 7.3 输入与按钮

- 输入背景：`surface-card #FFFFFF`；
- 输入边框：`border-default #C9DEE8`；
- 输入聚焦：`glacier-500 #3B91AE`边框，加`ice-cyan`低透明度焦点环；
- 主按钮：`glacier-500 #3B91AE`；
- 主按钮Hover：`glacier-600 #25748F`；
- 主按钮Active：`glacier-700 #1E5A70`；
- 主按钮文字：`#FFFFFF`。

### 7.4 右侧预览卡

- 主表面：`rgba(255,255,255,0.88)`；
- 边框：`1px solid #C9DEE8`；
- 圆角：`20px`；
- 阴影：`0 20px 50px rgba(8,28,38,0.12)`；
- 顶部强调线可使用`Glacier Flow`渐变，高度不超过2px；
- 卡片内部状态色必须保持低饱和、小面积。

---

## 8. Spacing 与尺寸

所有间距使用 `design.md` 的4px体系。

| 元素 | 规格 |
|---|---|
| 顶部导航高度 | `64px` |
| 模型标签高度 | `28px` |
| 输入框高度 | `52px` |
| 主按钮高度 | `52px` |
| 主按钮水平内边距 | `24px` |
| 输入框水平内边距 | `16px` |
| 输入/按钮间距 | 桌面`8px` |
| 主按钮最小宽度 | `120px` |
| 输入组合圆角 | `10px` |
| 视觉卡内边距 | `24px` |
| 视觉卡内部区块间距 | `16–24px` |
| 页面模块安全间距 | `32px`以上 |

输入框和按钮必须等高。主要点击区域不得小于`44×44px`。

---

## 9. CTA 层级

### 9.1 Primary CTA：开始评估

主CTA与股票代码输入框组合出现，是Hero中唯一实底按钮。

行为要求：

- 输入合法六位A股代码后提交；
- 空值或格式错误时在输入框下方显示错误，不跳转；
- 提交过程中显示`正在评估…`并避免重复提交；
- 支持键盘Enter提交；
- 不使用“立即赚钱”“获取买入建议”等文案。

### 9.2 Secondary CTA：查看评分规则

- 使用文字按钮或轻量描边按钮；
- 可配向右箭头；
- 视觉权重明显低于主CTA；
- 目标是打开规则说明或滚动到后续规则区，本设计不定义后续区块。

### 9.3 CTA 优先级规则

```text
开始评估 > 查看评分规则 > 导航中的其他链接
```

- Hero中不增加第三个CTA；
- 不使用并列双实底按钮；
- 风险说明不是CTA；
- 右侧评分预览不可点击，避免出现虚假的第二入口。

---

## 10. 右侧视觉资产要求

### 10.1 资产类型

使用“产品结果预览卡 + 抽象冰川趋势背景”的组合，而不是人物照片、真实股票截图或装饰性3D冰山。

预览卡需要表达产品的四层结果：

1. 硬性过滤；
2. 候选评分；
3. 初始止损；
4. 风险仓位。

建议预览内容：

```text
趋势回调转强评估

82 / 95            +5 待确认
候选 · 标准交易

硬性过滤           通过
趋势质量            20 / 25
回调质量            15 / 20
重新转强            25 / 25

初始止损            ¥19.00
计划最大风险        ¥500
计划仓位            500股
```

数据仅用于展示信息结构，必须标注为“示例结果”，不能绑定真实股票名称或暗示实际建议。

### 10.2 抽象图形

卡片背景或后方可使用：

- 1px冰蓝网格；
- 一条低透明度趋势折线；
- 三至四层类似冰层等高线的细线；
- 非对称但平衡的冷色雾面形状；
- 少量数据节点。

限制：

- 网格透明度不超过12%；
- 趋势线不使用红绿，不模拟明确买卖点；
- 不使用蜡烛K线作为主要装饰；
- 不使用大面积发光；
- 资产整体对比度低于左侧主标题和CTA。

### 10.3 资产交付规格

- 优先使用可缩放矢量或界面原生图形；
- 若必须使用位图，桌面资产至少`1200×1200px`，支持2倍屏；
- 背景必须可在浅色表面自然融合；
- 资产不能包含无法编辑的正文文案；
- 预览卡内容应由界面组件排版，背景装饰才作为独立资产；
- 不依赖资产传达关键产品信息。

### 10.4 动效建议

首版可以完全静态。若后续加入动效，仅允许：

- 预览卡首次进入时淡入并上移`8px`；
- 评分分段条在首次出现时从左至右展开；
- 数据节点进行一次性、低幅度出现；
- 总时长不超过`600ms`；
- 支持系统“减少动态效果”。

禁止持续漂浮、闪烁行情数字和循环背景流光。

---

## 11. Responsive Behavior

### 11.1 大桌面 `>= 1440px`

- 内容最大宽度保持`1280px`，不继续拉宽；
- 左右分栏保持6/1/5；
- 主标题字号`52px`；
- 预览卡宽度约`500px`；
- 首屏上下留白可增加到`80px`。

### 11.2 标准桌面 `1200–1439px`

- 左右边距`32px`；
- 仍保持双栏；
- 右侧资产宽度`440–480px`；
- 主标题字号可保持`48–52px`。

### 11.3 小型笔记本 `960–1199px`

- 双栏调整为左侧7列、右侧5列；
- 主标题字号降为`44/54`；
- 预览卡适当缩小，不裁切核心数据；
- 输入组合仍保持横向；
- 若内容高度超过首屏，允许自然滚动，不压缩垂直间距。

### 11.4 平板 `768–959px`

- 切换为单列；
- 左侧文案和输入区居中或保持左对齐，推荐左对齐；
- 右侧预览卡移动到输入区下方；
- 预览卡最大宽度`560px`；
- Hero不强制满屏高度；
- 主标题字号`40/50`；
- 上下区块间距`48px`。

### 11.5 手机 `< 768px`

- 页面左右边距`16px`；
- 顶部导航高度`56px`；
- 主标题字号`34/42`，不超过三行；
- 副标题字号`16/26`；
- 输入框和主按钮上下排列，间距`12px`；
- 输入框、主按钮宽度均为100%；
- 主按钮高度`52px`；
- 次要CTA放在主按钮下方，居左或居中但全站保持一致；
- 右侧视觉改为简化预览卡，仅保留总分、硬性过滤、风险和仓位；
- 隐藏纯装饰网格和部分等高线，减少拥挤；
- 风险说明放在预览卡之前，确保无需滚动太远即可看到；
- 不使用固定底部CTA。

### 11.6 极窄手机 `< 360px`

- 主标题字号降至`30/38`；
- 模型标签允许自然换行或缩短为`趋势回调转强模型`；
- 数字和单位不得断行；
- 预览卡内边距降至`16px`；
- 不横向滚动。

---

## 12. 状态设计

Hero 输入组件至少需要以下状态：

| 状态 | 表现 |
|---|---|
| 默认 | 白色输入框、冷灰蓝边框 |
| Hover | 边框提升为`glacier-300` |
| Focus | `glacier-500`边框与清晰焦点环 |
| 已输入 | 使用主文字色和等宽数字 |
| 错误 | 风险色边框、错误图标及具体文字 |
| 加载 | 主按钮显示加载状态，输入保持可见 |
| 数据服务异常 | 输入区下方持续显示原因和重试入口 |
| 禁用 | 保持文字可读，不仅降低透明度 |

错误示例：

```text
请输入六位A股代码，例如 600519。
```

服务异常示例：

```text
暂时无法获取行情数据，请稍后重试。不会使用过期数据生成评分。
```

---

## 13. Accessibility

- 主标题与背景对比度至少达到WCAG AA；
- 输入框必须有持续可见的`股票代码`标签；
- 输入错误与字段建立程序和视觉关联；
- 主按钮和次要CTA支持键盘访问；
- 焦点顺序：Logo/导航 → 股票代码 → 开始评估 → 查看评分规则；
- 所有点击区域至少`44×44px`；
- 预览卡中的状态不能只通过颜色表达；
- 装饰性趋势线和网格不被辅助技术朗读；
- 页面放大到200%时，输入、CTA和核心文案仍完整可用；
- 支持减少动态效果和高对比度环境。

---

## 14. 验收标准

- [ ] 首屏能够在5秒内说明产品用途、输入方式和主要输出；
- [ ] Hero只有一个实底主CTA；
- [ ] 用户无需滚动即可看到股票代码输入与开始评估按钮；
- [ ] 主标题、副标题和风险说明均使用冷静、不承诺收益的文案；
- [ ] 页面严格使用Glacier颜色、字体、间距、圆角和阴影令牌；
- [ ] 右侧资产展示结果结构，不展示真实股票推荐；
- [ ] 高分示例不会被误解为上涨概率；
- [ ] 桌面端双栏，平板和手机切换为单列；
- [ ] 手机端输入框和主按钮均占满宽度；
- [ ] 状态不只依赖颜色；
- [ ] 不存在霓虹、红绿闪烁、庆祝动画或收益暗示；
- [ ] 本设计没有扩展到Hero之外的页面区块；
- [ ] 本设计不包含生产代码。

---

## 15. 待审阅项

- [ ] 主标题是否采用“在买入之前，先把信号和风险算清楚”？
- [ ] 次要CTA是否保留“查看评分规则”？
- [ ] 右侧是否采用“评分结果预览卡”，而不是抽象插画？
- [ ] 桌面端是否接受左6列、呼吸区1列、右5列的布局？
- [ ] 手机端是否保留简化后的结果预览卡？
- [ ] Hero是否暂时保持静态，不加入任何入场动效？

确认后可进入低保真线框图或高保真视觉稿阶段，仍无需编写生产代码。
