---
title: 趋势回调转强评分页面 PRD
tags:
  - PRD
  - 交易系统
  - 趋势交易
  - 仓位管理
status: 待审阅
version: 0.2
related:
  - "[[趋势中的回调转强交易模型]]"
---

# 趋势回调转强评分页面 PRD

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 趋势回调转强评分页 |
| 当前版本 | PRD 0.2 |
| 文档状态 | 待审阅，不进入开发 |
| 规则来源 | 《趋势中的回调转强交易模型》0.2 |
| 目标市场 | A股，日线级别，中短期波段 |
| 核心输入 | 股票代码 |
| 核心输出 | 硬性过滤结果、评分、风险等级、计划止损和建议仓位 |

> [!warning] 产品边界
> 页面用于执行既定规则、减少主观随意性，不预测收益，不构成自动交易或收益保证。页面不直接连接券商下单。

---

## 2. 产品背景与问题

用户在准备买入股票时，需要同时检查趋势、回调质量、重新转强、市场环境、事件风险和账户风险。纯人工检查存在以下问题：

- 指标计算耗时，容易算错或漏项；
- 相似价格指标可能被重复理解为多个独立信号；
- 情绪、事件和组合风险容易在临近下单时被忽略；
- 评分与仓位脱节，容易出现“高分就重仓”；
- 次日开盘跳空后，原有评分和仓位可能已经失效；
- 事后难以还原当时使用的数据、回答和规则版本。

产品需要把交易模型转化为一条固定流程：

```text
输入股票代码
→ 校验股票和行情数据
→ 自动执行硬性过滤
→ 用户回答无法自动取得的项目
→ 自动计算可评分项
→ 显示候选评分和缺失项
→ 输入账户参数并计算计划仓位
→ 次日补充实际开盘价
→ 生成最终执行结论
```

---

## 3. 产品目标

### 3.1 核心目标

1. 用户只需先输入股票代码，即可启动完整评估。
2. 能由行情计算的条件全部自动计算，不要求用户目测均线、MACD或涨跌幅。
3. 暂时无法自动取得的条件尽可能转化为明确的单选题或多选题。
4. 硬性过滤与评分严格分开；任何硬性否决都不能被高分覆盖。
5. 页面给出可核查的逐项得分，而不是只显示一个总分。
6. 仓位按止损距离和账户风险计算，而不是按评分直接给出资金比例。
7. 区分“收盘后候选评估”和“次日开盘执行确认”，避免使用未来数据。
8. 明确展示本次使用的数据日期、规则版本和计算依据，保证结果可核查。

### 3.2 非目标

本期不包括：

- 自动下单、撤单或券商账户同步；
- 分钟级、盘中实时交易信号；
- 超跌反弹、打板、基本面或其他策略模型；
- AI自行修改评分权重和交易参数；
- 自动承诺胜率、目标价或预期收益；
- 多股票批量扫描和排行榜；
- 完整持仓管理后台。
- 历史评估记录和Markdown导出。

---

## 4. 目标用户与使用场景

### 4.1 目标用户

- 使用A股日线进行波段交易；
- 已有候选股票，希望下单前进行规则化检查；
- 能获得股票近100个交易日的开盘价和收盘价；
- 愿意填写事件、情绪和账户风险等无法由当前行情直接判断的信息。

### 4.2 主要使用场景

#### 场景A：收盘后评估候选股票

用户在交易日收盘后输入代码。页面使用最新完整交易日数据自动检查趋势、回调和转强条件，再让用户回答事件和执行相关问题，形成候选评分与次日计划。

#### 场景B：次日开盘前后确认是否执行

页面加载昨日保存的候选评估。用户输入实际或预计开盘价，或由数据源自动取得开盘价。页面补齐跳空得分、重新检查止损距离并计算最终股数。

#### 场景C：硬性条件失败

页面明确显示“不入场”及失败原因。仍可查看全部自动计算指标，但不能生成“建议买入”或有效仓位。

#### 场景D：数据不足

页面显示缺失数据和受影响项目，不用默认值伪造分数。若关键数据不足以完成趋势硬性过滤，则停止评估。

---

## 5. 核心产品规则

### 5.1 两阶段评估

#### 阶段一：候选评估（T日收盘后）

- 使用最新一个已完成交易日 `T` 的数据；
- 自动评分与用户问卷完成后，展示候选得分；
- “T+1开盘跳空不超过2%”尚未发生，标记为“待确认”，不提前加分；
- 展示格式为：`当前已得 X 分 / 已判定 Y 分，另有 Z 分待确认`；
- 可以生成次日计划，但不能把候选结论标记为最终可执行。

#### 阶段二：执行确认（T+1日）

- 获取或输入 `T+1` 实际开盘价；
- 补齐最后5分执行项；
- 按实际入场价重新计算止损距离、风险收益和计划股数；
- 若高开超过2%、止损距离超过8%或其他硬性条件发生变化，最终结论为“不执行”；
- 只有本阶段通过后，页面才显示最终风险档位和最终建议股数。

### 5.2 三种条件状态

所有条件只能处于以下状态之一：

| 状态 | 含义 | 评分处理 |
|---|---|---|
| 通过 | 数据或用户答案明确满足条件 | 获得对应分数 |
| 不通过 | 明确不满足条件 | 0分；硬性条件则直接否决 |
| 未知/待确认 | 无数据、用户不确定或时间尚未到 | 不加分，并单独提示完整度 |

不得把“未知”显示成“不通过”，也不得把“未知”默认视为通过。

### 5.3 评分和建议关系

- 硬性过滤不通过：结论固定为“不入场”，不显示有效风险仓位；
- 硬性过滤通过、最终分数 `< 70`：不交易；
- 最终分数 `70–79`：允许试仓，基础单笔风险0.25%；
- 最终分数 `80–89`：标准交易，基础单笔风险0.50%；
- 最终分数 `90–100`：高匹配交易，基础单笔风险最高0.75%。

分数不是上涨概率。页面不得使用“70%成功率”“强烈买入”等可能造成误解的表达。

---

## 6. 页面信息架构

页面采用单页、分步骤展开的结构。

### 6.1 页面顶部：股票代码输入

必备元素：

- 页面标题：`趋势回调转强评分`；
- 股票代码输入框；
- 主按钮：`开始评估`；
- 规则版本：`模型 0.2`；
- 数据更新时间说明。

输入框要求：

- 支持 `600519`、`000001`、`300750`、`688981` 等六位代码；
- 可兼容 `600519.SH`、`000001.SZ`，提交后统一标准化；
- 输入时移除首尾空格；
- 非六位数字或无法识别的证券显示明确错误；
- 股票名称必须由数据源返回，不能让用户仅凭代码继续，以避免输错股票。

### 6.2 股票摘要卡

成功提交后显示：

- 股票名称、代码、市场；
- 最新完整交易日；
- 最新开盘价、收盘价；
- 行情数据条数；
- 复权方式；
- 当前阶段：候选评估 / 执行确认；
- 数据完整度状态。

### 6.3 硬性过滤区

分为：

1. 自动检查；
2. 用户确认；
3. 总体结论。

自动检查在数据返回后立即运行。用户确认使用选择题。任一硬性项失败时，顶部固定显示红色“不入场”，并列出原因。

### 6.4 评分区

显示五个评分模块：

- 个股趋势质量：30分；
- 回调质量：25分；
- 重新转强信号：35分；
- 市场环境：10分。

技术指标合计90分。风险收益、次日高开和交易纪律属于硬性过滤，不参与加分。

每个模块显示：

- 已得分 / 模块总分；
- 通过、不通过、未知的项目数量；
- 可展开的逐项判断；
- 实际指标值、阈值、数据日期和数据来源；
- 自动判断或用户回答标识。

### 6.5 风险仓位区

仅在硬性过滤通过后启用。包括：

- 账户净值；
- 当前全部未平仓初始风险；
- 当前该行业持仓市值；
- 当前该股票持仓市值；
- 最近是否连续3笔完整止损；
- 计划或实际入场价；
- 系统计算的初始止损价；
- 单股风险、允许亏损金额、理论股数；
- 经过100股取整、单股上限、行业上限、组合风险上限后的最终股数；
- 限制仓位的具体原因。

### 6.6 结果与行动区

结果卡必须回答五个问题：

1. 是否通过硬性过滤？
2. 当前分数是多少，仍有多少分待确认？
3. 当前结论是“不入场、候选观察、允许试仓、标准交易”中的哪一种？
4. 如果次日执行，哪些条件会取消交易？
5. 在当前账户参数下，最多计划买多少股、最多承担多少计划亏损？

首版操作仅保留：`次日确认执行`与`重新评估`。历史保存和Markdown导出不进入首版。

---

## 7. 页面草图

```text
┌──────────────────────────────────────────────────────────────┐
│ 趋势回调转强评分                                     模型0.2 │
│ [ 输入股票代码，例如 600519                  ] [开始评估]    │
└──────────────────────────────────────────────────────────────┘

┌ 股票摘要 ────────────────────────────────────────────────────┐
│ 贵州茅台 600519.SH | 数据日 2026-08-18 | 前复权 | 100日     │
│ 当前阶段：T日收盘后候选评估              数据完整度：85%     │
└──────────────────────────────────────────────────────────────┘

┌ 硬性过滤 ────────────────────────┐ ┌ 当前结论 ──────────────┐
│ 自动检查  8/8                    │ │ 候选观察                │
│ 用户确认  3/4，尚有1项未回答     │ │ 已得 72 / 已判定 90     │
│ [展开查看每一项]                 │ │ 另有10分未知或待确认     │
└──────────────────────────────────┘ └─────────────────────────┘

┌ 需要你确认 ──────────────────────────────────────────────────┐
│ 未来5个交易日是否有财报、解禁或重大事件？                    │
│ ( ) 没有  ( ) 有  ( ) 不确定                                │
│                                                              │
│ 这笔交易是否来自事先计划？                                   │
│ ( ) 是  ( ) 临时起意                                        │
└──────────────────────────────────────────────────────────────┘

┌ 评分明细 ────────────────────────────────────────────────────┐
│ 趋势 24/30 | 回调 19/25 | 转强 29/35 | 环境 10/10          │
│ [逐项指标值、阈值、通过状态和来源]                           │
└──────────────────────────────────────────────────────────────┘

┌ 风险仓位 ────────────────────────────────────────────────────┐
│ 账户净值 [100000]  计划入场价 [20.00]  自动止损 [19.00]      │
│ 单笔风险0.50% | 允许亏损500元 | 理论500股 | 最终500股        │
│ [次日确认执行] [重新评估]                                   │
└──────────────────────────────────────────────────────────────┘
```

移动端按以上区块纵向排列；评分总览和结论优先于详细指标表。

---

## 8. 硬性过滤详细需求

### 8.1 自动硬性过滤

| 编号 | 条件 | 输入数据 | 判定规则 | 失败处理 |
|---|---|---|---|---|
| HF-A01 | 股票代码有效 | 证券基础信息 | 能识别唯一A股证券 | 停止评估 |
| HF-A02 | 行情不少于100日 | 日线日期 | 连续可用记录 `>= 100` | 停止评估 |
| HF-A03 | 日期无重复、关键字段无缺失 | O/C行情 | 日期唯一，O/C为合法正数 | 停止评估 |
| HF-A04 | 使用一致复权口径 | 行情元数据 | 全部记录为同一复权方式 | 停止评估 |
| HF-A05 | 使用完整日K | 最新日期/市场时间 | T日已收盘；盘中默认使用T-1 | 使用上一完整日并提示 |
| HF-A06 | `C > MA60` | 收盘价 | 严格大于 | 不入场 |
| HF-A07 | `MA20 > MA60` | 收盘价 | 严格大于 | 不入场 |
| HF-A08 | `MA20(T) > MA20(T-5)` | 收盘价 | 严格大于 | 不入场 |
| HF-A09 | 近10日最低收盘未低于MA60超过2% | 收盘价 | 每日 `C >= MA60 × 0.98` | 不入场 |
| HF-A10 | 止损距离有效 | 入场价/止损价 | `0 < (E-S)/E <= 8%` | 不执行 |
| HF-A11 | 压力位空间有效 | 入场价/止损价/60日最高收盘 | 潜在空间 `>= 2R` | 不执行 |
| HF-A12 | 次日未过度高开 | T日收盘/T+1开盘 | 高开幅度 `<= 2%` | 超过则不执行；开盘前待确认 |

`HF-A10` 在用户确认计划入场价后运行，并在次日实际开盘价出现后再次运行。

### 8.2 用户选择题：硬性过滤

问题必须使用固定选项，不能只提供自由文本。

#### Q-H01：证券交易状态

题目：`这只股票目前是否存在ST、退市整理、长期停牌或明显无法正常成交的情况？`

- `没有，可以正常交易` → 通过；
- `存在上述任一情况` → 不通过；
- `不确定` → 未知，阻止生成可执行结论。

如果以后接入证券状态数据，本题自动完成，仅允许用户查看依据。

#### Q-H02：近期事件风险

题目：`未来5个交易日内是否存在财报、解禁或其他你不愿承担的重大事件风险？`

- `没有已知风险事件` → 通过；
- `存在风险事件` → 不通过；
- `不确定，尚未检查` → 未知，阻止生成可执行结论，并提示先检查公告。

#### Q-H03：交易是否来自计划

题目：`这笔交易是否来自收盘前已经制定的选股或观察计划？`

- `是，属于计划内交易` → 通过；
- `否，是临时起意` → 不通过。

#### Q-H04：当前交易动机

题目：`以下哪一项最符合你现在想买入的主要原因？`

- `条件符合事先写好的规则` → 通过；
- `担心继续上涨而错过` → 不通过；
- `想尽快赚回之前的亏损` → 不通过；
- `连续亏损后想加大仓位` → 不通过；
- `说不清楚` → 不通过。

#### Q-H05：组合风险额度

优先通过账户参数自动计算。无法取得账户数据时显示：

题目：`当前账户是否仍有足够的单股、行业和总风险额度？`

- `三项额度都充足` → 通过；
- `单只股票仓位将超限` → 不通过；
- `所属行业仓位将超限` → 不通过；
- `全部持仓风险将超限` → 不通过；
- `不确定` → 未知，必须填写账户风险参数后继续。

### 8.3 硬性失败后的页面行为

- 立即在结果卡显示“不入场”；
- 显示全部失败项，不只显示第一项；
- 自动评分仍可计算并供用户学习，但使用灰色样式，明确标注“评分不改变否决结论”；
- 仓位结果固定为0股；
- 允许保存评估记录；
- 不显示“建议买入”“胜率”“机会等级”等措辞。

---

## 9. 自动评分详细需求

### 9.1 个股趋势质量：30分

| 编号 | 自动条件 | 分值 | 展示值 |
|---|---|---:|---|
| SC-T01 | `C(T) > MA20(T)` | 6 | C、MA20、差值百分比 |
| SC-T02 | `MA5(T) > MA10(T)` | 6 | MA5、MA10 |
| SC-T03 | `MA10(T) > MA20(T)` | 6 | MA10、MA20 |
| SC-T04 | `MA20(T)/MA20(T-5)-1 >= 1%` | 6 | 实际斜率 |
| SC-T05 | `0 < 20日收益率 <= 20%` | 6 | 实际20日收益率 |

全部由近100日收盘价自动计算。

### 9.2 回调质量：25分

| 编号 | 自动条件 | 分值 | 展示值 |
|---|---|---:|---|
| SC-P01 | 最近3至10日存在至少连续2日回落或横盘 | 6 | 命中日期、连续天数 |
| SC-P02 | 回调幅度为2%至8% | 7 | 高点日、低点日、幅度 |
| SC-P03 | 回调最低收盘价距离当日MA20不超过3% | 6 | 最低收盘、对应MA20、距离 |
| SC-P04 | 回调期间收盘价均未低于MA60 | 6 | 最小安全距离 |

“横盘”的程序定义需要在开发前确认。建议初始定义为：当日收盘相对前一日变化绝对值不超过0.5%。该参数必须写入规则配置，不能隐藏在代码中。

### 9.3 重新转强：35分

| 编号 | 自动条件 | 分值 | 展示值 |
|---|---|---:|---|
| SC-R01 | `C(T) > O(T)` | 5 | O、C、日内涨幅 |
| SC-R02 | `C(T) > C(T-1)` | 5 | 今日、昨日收盘 |
| SC-R03 | 重新站上MA5 | 8 | C、MA5、前三日状态 |
| SC-R04 | `C(T)` 高于T-1至T-3最高收盘 | 9 | C、前三日最高收盘 |
| SC-R05 | MACD柱连续2日增大或由负转正 | 8 | 最近3日MACD柱值 |

MACD统一使用收盘价和 `12、26、9` 参数。页面必须显示参数。

### 9.4 市场环境：10分

首版固定使用中证全指（000985.SH）并自动判断：

| 编号 | 条件 | 分值 |
|---|---|---:|
| SC-M01 | 基准指数收盘价高于MA20 | 4 |
| SC-M02 | 基准指数MA20最近5日向上 | 4 |
| SC-M03 | 基准指数5日收益率为正 | 2 |

指数数据不可用时10分记为未知，不显示市场选择题。

### 9.5 模块最低要求

- 技术指标合计至少 `65 / 90`；
- 重新转强至少 `21 / 35`；
- “重新站上MA5”或“突破前三日最高收盘”至少满足一项。

总分达到70但技术门槛不足时显示“候选观察”；重新转强门槛或关键触发器不足时显示“等待转强”。

---

## 10. 用户输入与选择题交互

### 10.1 提问原则

- 自动可计算的项目不询问用户；
- 外部数据取不到时才显示替代问题；
- 每题都必须提供“否”或“不确定”，不能诱导用户选择通过；
- 硬性题与加分题分区展示；
- 硬性题必须全部回答才能生成可执行结论；
- 加分题允许选择“不确定”，但对应项目不加分；
- 用户修改答案后，结果和仓位必须实时重新计算；
- 保存记录时保留题目、答案和回答时间。

### 10.2 账户风险输入

首次使用风险仓位功能时，要求填写：

| 字段 | 必填 | 默认值/规则 |
|---|---|---|
| 账户净值 | 是 | 无默认值，必须大于0 |
| 已使用的全部未平仓初始风险 | 是 | 默认0元，但需用户确认 |
| 当前该股票持仓市值 | 是 | 默认0元，但需用户确认 |
| 当前该行业持仓市值 | 是 | 默认0元，但需用户确认 |
| 是否刚连续3笔完整止损 | 是 | 单选：是/否 |
| 风险缓冲 | 否 | 默认10%，可在设置中调整 |

账户数据可保存在本地设置中；每次计算前显示最近更新时间，防止使用过期净值或持仓。

### 10.3 入场价格输入

- 候选阶段默认使用最新收盘价作为“估算入场价”，但必须明确标注为估算；
- 用户可以输入计划限价；
- 执行阶段优先自动读取T+1开盘价，取不到时允许用户输入实际开盘价或实际成交价；
- 用户手动输入的价格必须标记来源，并保留修改记录；
- 实际成交价与计划价格不同后，仓位必须重新计算。

---

## 11. 止损与仓位计算需求

### 11.1 初始止损

当前仅有开盘/收盘数据时：

```text
回调参考结构价 = 回调期间（最近3至10日）最低收盘价
初始止损价 S = 回调参考结构价 × 0.99
```

页面显示“临时收盘价止损模型”标签，并提示补齐最高价、最低价后结果会变化。

未来补齐最高价、最低价后：

```text
结构止损价 = 回调期间最低价 - 0.3 × ATR14
初始止损价 S = 结构止损价
```

系统必须记录本次使用的是哪一种止损算法。

### 11.2 风险额度

```text
基础风险比例 = 按最终分数取得0%、0.25%、0.50%或0.75%
调整后风险比例 = 连续3笔止损时，基础风险比例 × 50%
允许风险总额 = 账户净值 × 调整后风险比例
可用组合风险 = 账户净值 × 2% - 当前未平仓初始风险
本次允许亏损金额 = min(允许风险总额, 可用组合风险) × (1 - 风险缓冲)
```

若可用组合风险小于或等于0，最终仓位为0股。

### 11.3 股数计算

```text
每股风险 R = 实际或计划入场价 E - 初始止损价 S
风险约束股数 = floor(本次允许亏损金额 / R / 100) × 100
单股市值上限股数 = floor((账户净值 × 15% - 当前该股持仓市值) / E / 100) × 100
行业市值上限股数 = floor((账户净值 × 30% - 当前行业持仓市值) / E / 100) × 100
最终股数 = max(0, min(风险约束股数, 单股市值上限股数, 行业市值上限股数))
```

结果必须同时显示：

- 允许亏损金额；
- 每股风险和止损百分比；
- 三种上限分别对应的股数；
- 最终股数；
- 哪一项成为实际限制；
- 预计买入市值及其占账户净值比例。

最终股数不足100股时显示“风险额度不足，放弃交易”，不得建议缩短止损以增加股数。

---

## 12. 结果展示规范

### 12.1 结论标签

| 状态 | 标签 | 颜色建议 | 说明 |
|---|---|---|---|
| 硬性失败 | 不入场 | 红色 | 列出所有失败条件 |
| 数据或回答未完成 | 待补充 | 灰色 | 不生成可执行仓位 |
| 分数低于70 | 不交易 | 红色 | 条件不匹配模型 |
| 70–79 | 允许试仓 | 黄色 | 风险比例0.25% |
| 80–89 | 标准交易 | 蓝色 | 风险比例0.50% |
| 90–100 | 高匹配 | 绿色 | 风险比例最高0.75% |
| 尚无T+1开盘价 | 候选，待次日确认 | 紫色 | 不是最终执行结论 |

颜色不能作为唯一信息载体，必须同时显示文字和图标。

### 12.2 评分完整度

必须同时显示以下三个数字：

```text
已得分：满足条件获得的分数
已判定分：已有明确通过或不通过结果的总分值
待确认分：因缺数据、用户不确定或时间未到而尚未判定的分值
```

示例：`已得72分 / 已判定90分 / 待确认10分`。

禁止在待确认项目存在时，仅显示“72分”而不解释分母状态。

### 12.3 可解释性

每一个自动结果都能展开查看：

- 公式；
- 本次实际值；
- 通过阈值；
- 使用的数据日期；
- 自动数据或用户输入来源；
- 获得分数；
- 未通过时的简短原因。

---

## 13. 保存、复盘与导出（首版不实现）

### 13.1 后续保存内容

每条评估至少保存：

- 评估ID；
- 股票代码和名称；
- 创建时间、信号日期T、执行确认日期；
- 模型版本和参数版本；
- 行情数据截止日期和复权方式；
- 所有自动指标原始值；
- 所有硬性过滤结果；
- 用户问题及答案；
- 各评分项和总分；
- 账户参数快照；
- 计划与实际入场价；
- 止损价、每股风险、最终股数；
- 候选结论和最终结论；
- 数据缺失与警告信息。

### 13.2 后续Markdown摘要

输出Markdown格式，便于粘贴到Obsidian交易日志。建议结构：

```markdown
## 入场评估

- 股票：贵州茅台（600519.SH）
- 信号日期：2026-08-18
- 模型版本：0.2
- 硬性过滤：通过
- 候选得分：82 / 已判定95，执行项5分待确认
- 初始止损：19.00元
- 计划风险：500元
- 计划仓位：500股
- 次日取消条件：高开超过2%；止损距离超过8%；事件风险变化
```

首版不提供复制或自动写入Obsidian；以上格式仅保留为后续版本参考。

---

## 14. 数据需求与接口边界

### 14.1 P0必须数据

- A股代码与名称映射；
- 至少近100个交易日开盘价、收盘价；
- 交易日期；
- 复权口径说明；
- 数据更新时间。

### 14.2 P0用户确认数据

- ST、退市、停牌及流动性异常；
- 财报、解禁和重大事件；
- 组合持仓和风险额度；
- 交易计划和情绪状态。

### 14.3 P1建议接入数据

- 最高价、最低价、成交量、成交额；
- 行业指数行情；
- 股票行业分类；
- ST、停复牌、涨跌停状态；
- 财报、解禁及公告日历；
- 至少250个交易日复权行情。

P1数据接入后，对应问卷自动隐藏或改为只读校验，不应同时存在“自动结果”和“用户随意覆盖自动结果”。若确需人工更正，必须记录覆盖理由。

---

## 15. 状态与异常处理

| 场景 | 页面行为 |
|---|---|
| 股票代码格式错误 | 输入框下方提示正确示例，不发起计算 |
| 查无股票 | 提示无法识别，不保留上一次股票结果 |
| 行情少于100日 | 显示实际条数和缺口，停止评估 |
| 数据源失败 | 提供重试，不使用缓存冒充最新数据 |
| 使用缓存 | 明确显示缓存截止日期和生成时间 |
| 当日尚未收盘 | 默认使用上一完整交易日，并醒目标注 |
| 复权方式未知 | 停止评估 |
| 自动指标计算异常 | 标记具体指标，不生成总分 |
| 用户选择“不确定” | 标记未知；硬性项阻止执行，加分项记0且显示缺失 |
| 入场价小于等于止损价 | 不执行，仓位为0 |
| 止损距离超过8% | 不执行，仓位为0 |
| 账户风险额度不足 | 不执行，仓位为0 |
| T+1高开超过2% | 取消交易，保留候选评估记录 |
| 评分规则版本变化 | 旧记录保留原版本，不自动重算覆盖 |

---

## 16. 功能优先级

### P0：首版必须完成

- 单股票代码输入与标准化；
- 获取近100日开盘/收盘数据；
- 自动数据校验；
- 自动硬性趋势过滤；
- 价格类指标和评分自动计算；
- 无法自动取得项目的选择题；
- 候选评分与待确认分展示；
- 临时收盘价止损；
- 账户参数输入和风险仓位计算；
- T+1开盘执行确认；
- 逐项评分解释；
- 中证全指市场环境自动评分；
- 明确的数据时间与风险提示。

### P1：数据增强

- 最高价、最低价、ATR止损；
- 成交量与流动性过滤；
- 自动事件风险检查；
- 自动证券交易状态检查；
- 250日行情；
- 评估记录筛选和统计。

### P2：后续能力

- 批量候选股票扫描；
- 交易日志自动回填；
- 持仓后的退出信号跟踪；
- 回测结果和参数版本对比；
- 不同模型之间切换。

---

## 17. 非功能要求

### 17.1 准确性与可复现

- 同一规则版本、同一数据快照和同一用户答案必须得到相同结果；
- 金额保留2位小数，指标计算使用未四舍五入的原始值；
- 最终股数始终向下取整到100股；
- 所有日期使用交易所交易日和统一时区；
- 指标公式、阈值和参数必须配置化并带版本号。

### 17.2 性能

- 正常情况下，提交代码后3秒内展示股票摘要和自动检查结果；
- 数据加载超过1秒显示加载状态；
- 用户修改选择题或账户参数后，结果应在页面内即时更新。

### 17.3 隐私

- 账户净值和持仓参数默认只保存在本地；
- 不要求券商账号、交易密码或身份信息；
- 导出的评估摘要默认不包含账户总资产，可由用户主动选择包含。

### 17.4 可用性

- 桌面端优先，同时适配手机纵向页面；
- 关键结论不只依赖颜色；
- 公式默认折叠，实际结果与原因默认可见；
- 所有“未知”和“待确认”状态必须醒目，避免误认为通过。

---

## 18. 验收标准

### 18.1 股票输入

- [ ] 输入合法A股代码后能显示正确名称和最新完整数据日。
- [ ] 输入非法代码、非股票代码或空值时有明确提示。
- [ ] 代码带市场后缀和不带后缀时能标准化为同一证券。

### 18.2 自动判断

- [ ] 所有均线、收益率、MACD、回调和转强条件能显示实际值与阈值。
- [ ] 使用T日收盘数据生成信号，不使用T+1数据参与候选评分。
- [ ] 硬性趋势条件任一失败时，结论为“不入场”、仓位为0。
- [ ] 数据不足或复权未知时不生成看似完整的评分。

### 18.3 用户问题

- [ ] 所有无法自动判断的硬性项都有固定选择题。
- [ ] 硬性问题选择“不确定”时不能生成可执行结论。
- [ ] 加分问题选择“不确定”时不加分，并计入待确认或缺失说明。
- [ ] 修改答案后，评分和结论同步更新。

### 18.4 两阶段评分

- [ ] T日显示完整100分模型评分，但T+1高开硬性条件标为待确认。
- [ ] T+1录入开盘价后自动补齐执行分并重算仓位。
- [ ] T+1高开超过2%时最终结论为取消交易。
- [ ] 页面同时显示已得分、已判定分和待确认分。

### 18.5 仓位计算

- [ ] 仓位由账户风险和止损距离计算，不直接由评分映射为资金比例。
- [ ] 评分风险档、连续止损减半、2%组合风险上限、15%单股上限和30%行业上限同时生效。
- [ ] 最终股数向下取整至100股，并显示实际限制来源。
- [ ] 入场价不合理、风险额度不足或最终不足100股时返回0股。

### 18.6 首版范围

- [ ] 不保存历史评估记录。
- [ ] 不提供Markdown复制或Obsidian写入。
- [ ] 账户参数不上传服务器，也不持久化保存。

---

## 19. 已确认决策

- 首版仅支持六位A股代码；
- 个股与中证全指历史行情使用前复权公开行情；
- 盘中候选信号使用上一完整交易日，T+1开盘单独确认；
- 接受T日候选、T+1执行确认的两阶段流程；
- 横盘定义为单日涨跌幅绝对值不超过0.5%；
- 市场环境自动计算10分，不使用市场或行业选择题；
- 首版不接入公告、证券事件和行业指数接口；
- 账户参数不上传服务器，也不持久化；
- 首版不保存历史评估记录，不提供Markdown导出；
- 风险缓冲固定为10%；
- 结果文案使用“不入场 / 待补充 / 候选观察 / 等待转强 / 候选 / 允许试仓 / 标准交易 / 高匹配”。
