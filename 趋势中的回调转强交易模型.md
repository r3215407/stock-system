---
title: 趋势中的回调转强交易模型
tags:
  - 交易系统
  - 趋势交易
  - 仓位管理
status: 待审阅
version: 0.2
---

# 趋势中的回调转强交易模型

> [!warning] 模型定位
> 本模型适用于日线级别的中短期波段交易，目标是在已经形成上升趋势的股票中，等待价格回调后重新转强再入场。它不是“抄底”模型，也不用于下跌趋势中的超跌反弹。
>
> 评分只表示当前条件与模型的匹配程度，不代表上涨概率，更不能替代止损。模型正式使用前，应先回测并进行一段时间的模拟或小仓位验证。

## 一、模型原则

1. **先确认趋势，再等待回调，最后等待转强信号。**
2. **硬性过滤优先于评分。** 任一硬性条件不满足，即使评分很高也不交易。
3. **收盘后确认，下一交易日执行。** 信号日记为 `T`，计划在 `T+1` 日开盘或符合条件的价格成交，避免使用未完成的日K线。
4. **仓位由止损风险决定，评分只调整允许承担的风险。**
5. **不把相似指标当成多份独立证据。** 均线、MACD和布林带都源于价格，本模型限制它们的总权重。
6. **不在本模型中使用“触及布林下轨即买入”。** 该条件属于均值回归逻辑，应另建模型。

---

## 二、模型所需指标

### 2.1 个股价格指标

| 指标 | 定义或参数 | 用途 | 仅有近100日开盘/收盘能否计算 |
|---|---|---|---|
| `O`、`C` | 当日开盘价、收盘价 | 判断日内强弱、跳空和实际入场价 | ✅ 可以 |
| `MA5`、`MA10`、`MA20`、`MA60` | 收盘价简单移动平均 | 判断长期趋势、回调位置和转强 | ✅ 可以 |
| `MA20` 五日斜率 | `MA20(T) / MA20(T-5) - 1` | 判断中期均线是否真正向上 | ✅ 可以 |
| 5日、10日、20日收益率 | `C(T) / C(T-N) - 1` | 衡量短中期动量 | ✅ 可以 |
| 10日最高收盘价 | 最近10日收盘价最大值 | 计算回调幅度 | ✅ 可以 |
| 10日最低收盘价 | 最近10日收盘价最小值 | 临时估算结构止损 | ✅ 可以 |
| MACD柱值 | `DIF - DEA`；常用参数12、26、9 | 辅助判断下跌动能减弱、重新转强 | ✅ 可以 |
| 收盘波动率 | 最近20日每日收益率标准差 | 当前数据不足时估算波动风险 | ✅ 可以 |
| `H`、`L` | 当日最高价、最低价 | 识别真实回调低点和盘中止损 | ❌ 需要补充 |
| `ATR14` | 14日真实波幅均值 | 设置波动缓冲和移动止损 | ❌ 需要最高、最低价 |
| 成交量、成交额 | 当日及其20日均值 | 验证缩量回调、放量转强和流动性 | ❌ 需要补充 |

> [!important] 数据要求
> 所有价格必须采用一致的复权口径，建议使用前复权数据。除权除息若未处理，会产生虚假的暴跌、均线破位和止损信号。

### 2.2 市场与事件指标

| 指标 | 用途 | 当前个股开盘/收盘数据能否提供 |
|---|---|---|
| 基准指数收盘价、`MA20`及其斜率 | 判断市场是否支持做多 | ❌ 需另取指数行情 |
| 股票所属行业指数及20日收益率 | 判断板块相对强弱 | ❌ 需行业分类和行业行情 |
| 财报日期、解禁、停复牌、重大公告 | 排除跳空和事件风险 | ❌ 需事件数据或人工确认 |
| ST、退市风险、涨跌停状态 | 排除交易和流动性异常 | ❌ 需证券状态及涨跌停数据 |

### 2.3 建议补充数据的优先顺序

1. 最高价、最低价；
2. 成交量、成交额；
3. 复权因子或复权后的历史行情；
4. 基准指数和行业指数行情；
5. 财报、解禁、停复牌及重大公告日历。

近100个交易日可以运行第一版模型，但为了让 `MA60`、MACD和历史波动率更稳定，建议保存至少250个交易日的数据。

---

## 三、硬性过滤

以下条件必须全部通过。任何一项为“否”，本次不评分、不入场。

### 3.1 交易与数据条件

- [ ] 使用复权口径一致、无缺失和无重复日期的行情数据。
- [ ] 至少有100个连续交易日数据；正式版本建议至少250日。
- [ ] 股票可以正常交易，不是ST、退市整理、长期停牌或流动性明显不足的股票。
- [ ] 信号在 `T` 日收盘后确认，不使用 `T` 日盘中尚未完成的数据。
- [ ] 已能写出明确的计划入场价、初始止损价和失效条件。
- [ ] 入场价到止损价的距离大于0，且不超过入场价的8%。超过8%说明入场太晚或波动过大。
- [ ] 到最近60日最高收盘价的潜在空间不少于 `2R`。
- [ ] `T+1` 开盘价相对信号日收盘价的高开幅度不超过2%；开盘前该项记为待确认。

### 3.2 趋势条件

- [ ] `C(T) > MA60(T)`。
- [ ] `MA20(T) > MA60(T)`。
- [ ] `MA20(T) > MA20(T-5)`，即20日均线最近5日向上。
- [ ] 最近10日最低收盘价没有低于 `MA60` 超过2%。

以上条件用于确保这是上升趋势中的回调，而不是下跌趋势中的反弹。

### 3.3 事件与主观条件

- [ ] 未来5个交易日内没有已知财报、解禁或其他本人不愿承担的重大事件风险。
- [ ] 这笔交易来自事先制定的选股和入场计划，不是盘中临时起意。
- [ ] 不是因为害怕错过、急于回本或连续亏损后报复性交易。
- [ ] 当前组合仍有足够的单笔风险额度、总风险额度和行业额度。

> [!note] 可调整项
> “财报前5日”和“最大止损距离8%”是初始参数，后续应根据回测和实际风险偏好调整，但不能在某笔交易出现后临时放宽。

---

## 四、100分评分表

只有通过硬性过滤后才评分。评分只衡量技术形态与市场环境；止损距离、风险收益、次日高开和交易纪律不再加分。

### 4.1 个股趋势质量：30分

| 条件 | 分值 |
|---|---:|
| `C(T) > MA20(T)` | 6 |
| `MA5(T) > MA10(T)` | 6 |
| `MA10(T) > MA20(T)` | 6 |
| `MA20(T) / MA20(T-5) - 1 >= 1%` | 6 |
| 20日收益率为正，且不高于20%（避免过度追涨） | 6 |

### 4.2 回调质量：25分

先定义：

```text
回调幅度 = C(T)之前最近10日的最高收盘价 / 回调期间最低收盘价 - 1
```

| 条件 | 分值 |
|---|---:|
| 最近3至10个交易日内出现过至少连续2日回落或横盘 | 6 |
| 回调幅度位于2%至8%之间 | 7 |
| 回调最低收盘价与当日 `MA20` 的距离不超过3% | 6 |
| 回调期间所有收盘价均未低于 `MA60` | 6 |

说明：回调不足2%可能没有形成有效整理；超过8%可能代表趋势受损。这两个阈值需要用历史样本校准。

### 4.3 重新转强信号：35分

| 条件 | 分值 |
|---|---:|
| `C(T) > O(T)`，当日收阳 | 5 |
| `C(T) > C(T-1)` | 5 |
| `C(T)` 由下向上重新站上 `MA5(T)` | 8 |
| `C(T)` 高于此前3日最高收盘价 | 9 |
| MACD柱值连续2日增大，或当日由负转正 | 8 |

“由下向上重新站上MA5”定义为：`C(T) > MA5(T)` 且此前3个交易日中至少有一天 `C <= MA5`。

### 4.4 市场环境：10分

首版固定使用中证全指（000985.SH），由行情接口自动计算，不再要求用户主观选择板块强弱。

| 条件 | 分值 |
|---|---:|
| 基准指数收盘价高于其 `MA20` | 4 |
| 基准指数 `MA20(T) > MA20(T-5)` | 4 |
| 基准指数5日收益率为正 | 2 |

市场数据暂时不可用时，对应10分记为未知，不允许用户凭感觉补分。

### 4.5 模块最低要求

除总分外，必须同时满足：

```text
技术指标总分（趋势 + 回调 + 转强） >= 65 / 90
重新转强得分 >= 21 / 35
“重新站上MA5”或“突破此前3日最高收盘价”至少满足一项
```

如果总分达到70但技术面不足65，结论为“候选观察”；重新转强不足21或关键触发器未出现，结论为“等待转强”。

### 4.6 分数解释

| 总分 | 决策 | 风险等级 |
|---:|---|---|
| `< 70` | 不交易 | 0 |
| `70–79`且模块门槛通过 | 允许试仓 | 每笔承担账户净值的0.25%风险 |
| `80–89`且模块门槛通过 | 标准交易 | 每笔承担账户净值的0.50%风险 |
| `90–100`且模块门槛通过 | 高匹配交易 | 每笔最多承担账户净值的0.75%风险 |

高分只允许提高预先设定的风险额度，不允许取消止损，也不建议单笔风险超过账户净值的0.75%。上述比例是保守的初始值，应根据回测、最大回撤承受能力和实盘记录调整。

---

## 五、入场规则

### 5.1 信号确认

1. `T` 日收盘后运行硬性过滤和评分。
2. 硬性过滤全部通过且总分不低于70分，才生成次日交易计划。
3. 记录计划入场价、初始止损价、每股风险 `R`、目标股数和不成交条件。

### 5.2 次日执行

- 默认以 `T+1` 日开盘价作为预计成交价，实际成交后必须按实际价格重新计算仓位。
- 如果 `T+1` 开盘价比 `T` 日收盘价高2%以上，取消交易，不追高。
- 如果实际成交价使止损距离超过8%，取消交易。
- 如果开盘后股票直接涨停、无法正常成交或流动性异常，取消交易。
- 回测时必须使用 `T+1` 开盘价并计入手续费、滑点，不能按 `T` 日收盘价成交。

---

## 六、初始止损系统

### 6.1 完整行情版（补齐最高价、最低价后使用）

```text
结构止损价 = 回调期间最低价 - 0.3 × ATR14
初始止损价 S = 结构止损价
```

附加限制：

- 如果 `(E - S) / E < 2%`，可按2%最小风险距离重新评估，避免正常波动频繁触发止损；
- 如果 `(E - S) / E > 8%`，取消交易；
- 止损只能向有利方向移动，不能因为股价下跌而下移。

### 6.2 当前数据临时版（仅有开盘价、收盘价）

```text
参考结构价 = 回调期间（最近3至10日）最低收盘价
初始止损价 S = 参考结构价 × 0.99
```

由于没有盘中最高价和最低价，该止损只是近似值：

- 实盘可以把它作为下单止损参考；
- 回测无法知道日内是否先触发止损，只能采用“收盘确认、次日开盘退出”的保守统一规则；
- 补齐最高价和最低价后，应废止临时算法并重新回测，不能把两种结果直接比较。

---

## 七、仓位按止损风险计算

### 7.1 核心公式

```text
允许亏损金额 A = 账户净值 × 对应评分的单笔风险比例
每股初始风险 R = 实际入场价 E - 初始止损价 S
理论股数 Q = 向下取整(A / R)
```

A股通常以100股为一手，因此：

```text
实际股数 = floor(Q / 100) × 100
```

如果实际股数不足100股，则放弃该笔交易，不通过降低止损距离来凑足一手。

### 7.2 仓位上限

风险公式计算后，还必须同时受以下上限约束，最终取最小值：

- 单只股票市值不超过账户净值的15%；
- 同一行业全部持仓市值不超过账户净值的30%；
- 所有未平仓交易的初始风险合计不超过账户净值的2%；
- 连续出现3笔完整止损后，新交易的单笔风险比例减半；直到出现一次按规则盈利退出，或完成书面复盘后再恢复；
- 禁止因浮亏而临时加仓。若以后设计加仓系统，应单独计算新增风险。

### 7.3 示例

```text
账户净值 = 100,000元
模型评分 = 84分，对应单笔风险比例 = 0.50%
允许亏损金额 A = 100,000 × 0.50% = 500元
实际入场价 E = 20.00元
初始止损价 S = 19.00元
每股风险 R = 1.00元
理论股数 Q = 500 / 1 = 500股
交易市值 = 500 × 20 = 10,000元，占账户10%
```

500股既未超过15%的单股市值上限，也符合风险额度，因此最终仓位为500股。正式计算默认预留10%的风险缓冲，以覆盖手续费、滑点和跳空造成的偏差。

---

## 八、退出系统

退出按优先级执行：灾难风险/初始止损 → 趋势失效 → 移动保护 → 时间退出。任何退出信号都不能因为评分仍然较高而被忽略。

### 8.1 初始止损退出

完整行情与实盘版本：

- 价格触及初始止损价时执行止损；
- 若次日跳空低开越过止损价，以可成交价格尽快退出，并按实际损失记录；
- 不向下移动止损，不用补仓降低成本。

仅开盘/收盘数据的回测版本：

- 当日收盘价低于止损价，下一交易日开盘退出；
- 所有样本必须采用同一规则，不能对个别交易主观选择更好的成交价。

### 8.2 趋势失效退出

满足任一条件，在信号日收盘确认后，于下一交易日开盘退出：

- 连续2个交易日收盘价低于 `MA20`；
- `MA20 <= MA60`；
- 收盘价低于 `MA60` 超过2%。

### 8.3 盈利保护与移动止损

用初始每股风险 `R = E - S` 衡量盈利：

1. 浮盈未达到 `1R`：维持初始止损，不因小幅上涨随意抬高。
2. 收盘价首次达到 `E + 1R`：把保护止损提高到入场价附近；考虑费用后，可设为 `E × 1.002`。
3. 收盘价首次达到 `E + 2R`：启动移动退出；若收盘价连续2日低于 `MA10`，下一交易日开盘全部退出。
4. 若完整行情数据可用，可将第3条升级为：保护止损取“当前保护止损、`MA10`、持仓以来最高收盘价减去 `2 × ATR14`”三者中的最高值。

本版本不采用主观分批止盈，以便保持规则简单和便于回测。后续若比较发现分批退出能稳定降低回撤，再单独建立版本测试。

### 8.4 时间退出

- 入场后10个交易日仍未达到 `+1R`，且收盘价不高于入场价：下一交易日开盘退出；
- 最长持有40个交易日。到期仍未触发其他退出条件，下一交易日开盘退出；
- 财报等重大事件进入本人不愿承担的风险窗口时，按事先固定规则退出，不能临时猜测事件结果。

---

## 九、每笔交易必须记录的字段

```text
股票代码：
信号日期 T：
硬性过滤是否全部通过：
趋势质量得分 / 30：
回调质量得分 / 25：
重新转强得分 / 35：
技术指标合计 / 90：
市场环境得分 / 10：
模块最低要求是否通过：
总分 / 100：
计划入场价：
实际入场价 E：
初始止损价 S：
每股风险 R：
账户净值：
单笔风险比例：
允许亏损金额：
计划股数：
实际股数：
退出日期与价格：
退出原因：初始止损 / 趋势失效 / 移动保护 / 时间退出 / 事件退出
手续费与滑点：
最终收益金额：
最终收益（R倍数）：
是否完全按规则执行：
```

使用 `R倍数` 统一比较不同价格和不同仓位的交易：

```text
单股实际收益 = 退出价 - 入场价
收益R倍数 = 单股实际收益 / 初始每股风险R
```

---

## 十、回测与启用标准

### 10.1 回测要求

- 至少覆盖上涨、震荡和下跌三种市场阶段；
- 建议至少100至200笔交易样本，不只观察几只成功案例；
- 计入佣金、印花税、滑点、涨跌停无法成交和停牌影响；
- 信号使用 `T` 日收盘数据，成交使用 `T+1` 日开盘数据；
- 不在看完结果后逐笔修改参数；修改参数必须形成新版本并重新测试。

### 10.2 重点评估指标

- 总交易次数；
- 胜率；
- 平均盈利R、平均亏损R；
- 期望值：`胜率 × 平均盈利R - 败率 × 平均亏损R`；
- 最大连续亏损次数；
- 最大回撤；
- 不同评分区间的平均收益R；
- 不同市场环境下的表现；
- 规则执行率。

如果高分组没有比低分组表现出更好的平均收益、期望值或回撤特征，应调整或删除无效评分项，而不是继续增加指标。

### 10.3 正式使用前建议

1. 先检查指标计算和复权是否正确；
2. 用历史数据完成第一轮回测；
3. 使用20至30笔模拟交易检查实际执行问题；
4. 再用最低风险档进行小仓位验证；
5. 每累计30至50笔交易统一复盘一次，中途不因单笔盈亏改变规则。

---

## 十一、待审阅参数

以下参数没有唯一正确答案，需要在回测后确认：

- [ ] 回调观察窗口：3至10日是否合适？
- [ ] 合理回调幅度：2%至8%是否合适？
- [ ] 回调靠近 `MA20` 的容差：3%是否合适？
- [ ] 次日高开取消阈值：2%是否合适？
- [ ] 最大初始止损距离：8%是否合适？
- [ ] 评分入场线：70分是否合适？
- [ ] 三档单笔风险：0.25% / 0.50% / 0.75%是否合适？
- [ ] `+1R` 后移动到成本附近是否会过早止损？
- [ ] 10日未达到 `+1R` 的时间退出是否合适？
- [ ] 最长持有40日是否符合预期交易周期？

这些参数在版本确认后应固定。任何修改都记录版本号、修改日期、原因和回测结果。
