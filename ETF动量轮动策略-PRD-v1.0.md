---
title: Glacier Signal ETF 动量轮动策略 PRD
status: 待审阅
version: 1.0-draft
date: 2026-08-26
---

# Glacier Signal ETF 动量轮动策略 PRD

## 1. 产品摘要

| 项目 | 内容 |
|---|---|
| 功能名称 | ETF 动量轮动 |
| 页面名称 | ETF 动量轮动 |
| 建议路由 | `/etf-rotation` |
| 产品形态 | 无需用户操作的单用户模拟组合；页面只读展示运行结果 |
| 初始资金 | 固定为 ¥100,000.00，不提供页面修改入口 |
| 运行时间 | 每天北京时间 14:45 触发；非交易日识别后跳过交易计算 |
| 调度方式 | Vercel Cron，UTC 表达式 `45 6 * * *`，业务逻辑统一使用 `Asia/Shanghai` |
| 行情数据 | 每次运行即时获取近期行情；可正常获取时不持久化原始行情 |
| 业务数据 | Vercel Postgres 保存组合、持仓、交易、每日快照和任务日志 |
| 策略范围 | 固定 9 只 ETF，25 个价格点动量排名，只持有排名第 1 的 ETF |
| 交易性质 | 模拟记账，不连接券商，不执行真实委托 |
| 通知方式 | 每天触发后均发送邮件；成功、失败和非交易日跳过使用不同模板 |

## 2. 背景与目标

现有 Python 脚本已经实现了 ETF 池、动量排名、单标的轮动、模拟资金记账、组合收益记录和邮件通知，但依赖本地 Parquet/JSON 文件和硬编码邮箱配置，无法在 Vercel 的无状态运行环境中稳定工作，也没有可供用户查看持仓与历史表现的页面。

本期目标：

1. 将脚本规则产品化为每天自动运行的模拟 ETF 轮动策略。
2. 将初始资金固定为 100,000 元，并在 Postgres 中持续维护唯一组合的资金与持仓。
3. 不保存可随时重新拉取的原始行情，只保存无法从行情源重建的业务状态。
4. 新增只读的“ETF 动量轮动”页面，展示当前持仓、核心指标、收益率曲线、近 10 笔交易和 ETF 池。
5. 每次任务完成后发送结果邮件；任务失败时不改变持仓，并提供可追踪的错误信息。

## 3. 非目标

- 不连接券商，不自动下真实订单。
- 不允许用户在页面手动触发、暂停或修改策略。
- 不支持多个用户、多个账户或多个组合。
- 不支持用户修改初始资金、ETF 池、持仓数量、策略参数或邮件收件人。
- 不保存完整日线、实时行情或排名历史；这些数据在需要时重新获取。
- 不在首版加入手续费、税费、滑点、分红再投资或实际成交偏差。
- 不将模拟收益表述为真实收益或未来收益承诺。

## 4. 原脚本分析

### 4.1 策略参数

```text
ETF_POOL = 518880, 513100, 513350, 159915, 510180,
           159612, 513030, 159928, 513520
M_DAYS = 25
TARGET_NUM = 1
```

脚本为固定 ETF 池的横截面动量轮动，不是全市场选股。每次计算每只 ETF 的 25 点动量得分，按得分降序排列，只持有第 1 名。

### 4.2 动量得分

对每只 ETF 的 25 个价格点执行以下计算：

```text
y = ln(close)
x = 0, 1, ..., 24
slope = y 对 x 的一元线性回归斜率
annualized_momentum = exp(slope)^250 - 1
R² = 1 - SSE / ((n - 1) × sample_variance(y))
score = annualized_momentum × R²
```

其中前 24 个价格点来自已完成交易日的前复权收盘价，第 25 个价格点使用 14:45 获取的当日最新价。分数可为负数；当前脚本没有“分数必须为正”或空仓过滤，因此即使所有 ETF 得分为负，也会持有排名第 1 的 ETF。本期原样保留该行为。

### 4.3 轮动规则

```text
获取完整行情
→ 计算 9 只 ETF 的分数与排名
→ 目标 ETF = 排名第 1
→ 当前持仓不是目标 ETF：按当前价卖出
→ 当前无目标持仓：用全部可用资金按当前价买入目标 ETF
→ 计算并保存组合快照
→ 发送邮件
```

若当前持仓仍为第 1 名，只记录 `HOLD` 运行结果，不新增买卖交易。

### 4.4 原脚本需在实现时修正的问题

| 问题 | 本 PRD 处理方式 |
|---|---|
| 本地 Parquet 与 JSON 不适合无状态部署 | 行情即时拉取；组合业务状态迁移到 Postgres |
| 初始化历史区间是硬编码日期 | 每次动态获取不少于 24 个已完成交易日的数据 |
| 14:45 数据与 15:10 更新拆成两个本地任务 | 删除行情落盘与 15:10 更新任务；次日重新拉取近期数据 |
| 买入仅记录投入金额，没有明确数量 | 数据库记录等效持有份额 `quantity = amount / price`，允许小数份额以复刻脚本资金曲线 |
| 没有交易历史表 | 每笔 BUY/SELL 写入不可变交易记录 |
| 没有任务幂等保护 | 每个北京时间日期最多一个成功策略执行结果，数据库唯一约束防止重复成交 |
| 数据异常可能导致部分交易 | 所有行情校验在交易前完成，卖出、买入、快照在同一数据库事务中提交 |
| 邮件配置和授权码硬编码 | 全部改为服务端环境变量，任何密钥不得进入仓库、数据库或页面 |
| 邮件失败只打印日志 | 邮件状态写入任务日志，账本不回滚，可由后续告警或重试机制处理 |
| 异常读取 JSON 时会静默重置资金 | 不允许静默重置；数据库异常必须令任务失败并保留原状态 |

## 5. 核心业务规则

### 5.1 唯一组合与初始数据

系统只有一个组合，建议固定业务键为 `etf_momentum_v1`。数据库为空时，只允许初始化一次：

```json
{
  "start_date": "2026-06-08",
  "start_value": 100000.0,
  "history": [
    {
      "date": "2026-06-08",
      "total_value": 100000.0,
      "cumulative_return": 0.0,
      "annualized_return": 0.0
    }
  ]
}
```

对应的初始数据库状态：

- 开始日期：2026-06-08；
- 初始资产：¥100,000.00；
- 可用资金：¥100,000.00；
- 当前持仓：空；
- 2026-06-08 资产快照：总资产 ¥100,000.00，累计收益 0%，年化收益 0%；
- 初始化不是一笔交易，不进入近 10 笔历史交易。

初始化必须使用数据库事务与唯一键，禁止因部署、冷启动、重试或查询失败而重复初始化或重置已有组合。

### 5.2 行情获取口径

每次任务只获取本次计算所需数据：

1. 一次性获取全市场 ETF 实时快照，并从中读取 9 个目标代码的最新价、名称和行情时间。
2. 对每只 ETF 获取足够覆盖至少 24 个已完成交易日的前复权日线，建议请求最近 40 个自然日并按交易日过滤；不足时扩大时间区间重试一次。
3. 排除当日未完成日线，取当日之前最近 24 个有效收盘价。
4. 将 14:45 当日实时价作为第 25 个价格点。
5. 原始实时快照和历史日线只存在于本次函数内存，任务结束后不落库。

单只 ETF 必须同时满足：

- 有且仅有一个可用的当日最新价；
- 最新价为有限正数；
- 行情时间属于当前北京时间交易日，且不是明显过期快照；
- 有至少 24 个按日期升序、无重复的已完成交易日收盘价；
- 所有收盘价均为有限正数；
- 回归分母有效，分数为有限数值。

9 只 ETF 中任意一只校验失败，整次策略运行失败：不排名、不交易、不写资产快照。页面继续展示上一次成功运行结果，并明确标记“今日运行失败/数据未更新”。

### 5.3 交易日与执行时间

- Cron 每个自然日北京时间 14:45 触发，以满足“每天运行”的要求。
- 周六、周日和中国内地证券市场休市日直接标记为 `SKIPPED_NON_TRADING_DAY`，不获取行情、不交易、不新增资产快照，但发送当日跳过通知邮件。
- 调度表达式使用 UTC：`45 6 * * *`。
- 所有业务日期、页面时间和邮件时间使用 `Asia/Shanghai`；数据库时间戳使用 UTC 保存。
- 页面不提供“立即运行”“补跑”或“重新发送”按钮。

### 5.4 模拟成交与资金记账

首版严格复刻原脚本的按金额成交语义：

- 成交价：14:45 本次实时快照中的最新价；
- 买入金额：卖出完成后的全部可用资金；
- 买入数量：`买入金额 / 买入价`，允许小数份额；
- 卖出金额：`持有数量 × 卖出价`；
- 手续费、滑点和税费：均为 0；
- 同一次轮动先卖后买，两笔交易使用同一批行情快照；
- 每次买入后允许因数值精度产生小于 ¥0.01 的账面余量，但金额计算统一使用高精度十进制类型，禁止 JavaScript 浮点数直接参与入库金额运算。

交易动作：

| 当前状态 | 目标状态 | 动作 |
|---|---|---|
| 空仓 | 任意目标 ETF | BUY：使用全部可用资金买入目标 ETF |
| 持有目标 ETF | 目标 ETF 不变 | HOLD：不新增交易，更新当日资产快照 |
| 持有非目标 ETF | 新目标 ETF | SELL 旧 ETF，随后 BUY 新 ETF |
| 行情或计算失败 | 未知 | 不执行任何交易，保留原组合 |

### 5.5 幂等与一致性

1. 同一北京时间业务日期只能有一个有效执行键，例如 `etf_momentum_v1:2026-08-26`。
2. Cron 重试或并发请求必须由数据库唯一约束拦截，后到请求返回“已处理”，不得重复成交或重复发正常结果邮件。
3. 行情获取和校验在数据库事务之前完成。
4. 锁定组合记录后，在同一事务内完成 SELL、BUY、现金与持仓更新、每日快照和任务结果。
5. 事务失败则全部回滚，不允许出现“已卖未买”或“交易已写入但组合未更新”。
6. 邮件在数据库提交后发送。邮件失败不回滚已经完成的策略与账本，只更新任务的邮件状态。

## 6. 指标定义

所有页面指标以最近一次成功运行产生的资产快照和当前组合状态为准。页面不得在用户访问时用新的实时价偷偷改写 14:45 的账本口径。

| 指标 | 定义 |
|---|---|
| 总资产 | `可用资金 + Σ(持仓数量 × 最近一次成功运行价格)` |
| 可用资金 | 组合现金余额 |
| 持仓市值 | `Σ(持仓数量 × 最近一次成功运行价格)` |
| 仓位占比 | `持仓市值 / 总资产`；总资产为 0 时显示 `—` |
| 累计收益 | `总资产 / 100000 - 1` |
| 年化收益 | 按原脚本口径：`(1 + 累计收益)^(365 / 开始日至快照日的自然日数) - 1`；开始日当天为 0 |
| 日收益率 | 相邻两个有效交易日资产快照：`当日总资产 / 前一快照总资产 - 1` |
| 夏普比率 | `平均日收益率 / 日收益率标准差 × sqrt(250)`，无风险利率首版为 0 |

夏普比率规则：

- 使用所有按日期升序的有效资产快照，不填充周末和休市日；
- 至少需要 2 个日收益率且标准差大于 0；否则显示 `—` 和“数据不足”；
- 页面和邮件保留 2 位小数，数据库保存高精度原值；
- 不使用累计收益直接估算夏普比率。

## 7. 数据模型（Vercel Postgres）

原始行情不入库。建议保存以下五类业务数据。

### 7.1 `strategy_portfolios`：组合主表

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | uuid | 主键 |
| `strategy_key` | text unique | 固定 `etf_momentum_v1` |
| `start_date` | date | 固定 2026-06-08 |
| `start_value` | numeric(20,6) | 固定 100000 |
| `cash` | numeric(20,6) | 当前可用资金 |
| `created_at` | timestamptz | 创建时间 |
| `updated_at` | timestamptz | 最近一次账本更新时间 |

### 7.2 `strategy_positions`：当前持仓表

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | uuid | 主键 |
| `portfolio_id` | uuid | 所属组合 |
| `symbol` | varchar(6) | ETF 代码 |
| `quantity` | numeric(24,10) | 等效持有份额，允许小数 |
| `entry_price` | numeric(20,6) | 买入价 |
| `entry_amount` | numeric(20,6) | 初始投入金额 |
| `entry_at` | timestamptz | 买入时间 |
| `last_price` | numeric(20,6) | 最近一次成功运行价格 |
| `last_valued_at` | timestamptz | 最近估值时间 |

约束：同一组合与代码唯一；本策略正常状态最多只有一条持仓。

### 7.3 `strategy_trades`：不可变交易表

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | uuid | 主键 |
| `portfolio_id` | uuid | 所属组合 |
| `run_id` | uuid | 所属任务 |
| `side` | text | `BUY` 或 `SELL`；HOLD 不入表 |
| `symbol` | varchar(6) | ETF 代码 |
| `executed_at` | timestamptz | 模拟成交时间 |
| `price` | numeric(20,6) | 模拟成交价 |
| `quantity` | numeric(24,10) | 模拟成交份额 |
| `amount` | numeric(20,6) | 成交金额 |
| `realized_pnl` | numeric(20,6) nullable | 卖出实现盈亏；买入为空 |
| `cash_after` | numeric(20,6) | 成交后可用资金 |

### 7.4 `portfolio_daily_snapshots`：资产曲线表

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | uuid | 主键 |
| `portfolio_id` | uuid | 所属组合 |
| `snapshot_date` | date | 北京时间业务日期，与组合唯一 |
| `total_value` | numeric(20,6) | 总资产 |
| `cash` | numeric(20,6) | 可用资金 |
| `position_value` | numeric(20,6) | 持仓市值 |
| `cumulative_return` | numeric(18,10) | 累计收益率 |
| `annualized_return` | numeric(18,10) | 年化收益率 |
| `created_at` | timestamptz | 写入时间 |

### 7.5 `strategy_runs`：任务与审计日志

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | uuid | 主键 |
| `strategy_key` | text | 策略业务键 |
| `business_date` | date | 北京时间业务日期 |
| `idempotency_key` | text unique | 防重复执行键 |
| `status` | text | RUNNING / SUCCESS / FAILED / SKIPPED_NON_TRADING_DAY |
| `action` | text nullable | BUY / SELL_BUY / HOLD |
| `target_symbol` | varchar(6) nullable | 本次第 1 名 |
| `started_at` | timestamptz | 开始时间 |
| `finished_at` | timestamptz nullable | 结束时间 |
| `market_data_at` | timestamptz nullable | 行情快照时间 |
| `error_code` | text nullable | 稳定错误码 |
| `error_message` | text nullable | 已脱敏错误摘要 |
| `email_status` | text | PENDING / SENT / FAILED / NOT_REQUIRED |
| `email_sent_at` | timestamptz nullable | 邮件发送时间 |

不保存完整 9 只 ETF 的每日行情与排名。页面股票池的当前排名与分数仅展示最近一次成功运行响应中可选择保存的轻量结果时，才需要增加 `strategy_run_rankings` 表；本期默认不保存该表，页面访问时只展示固定股票池及最新可取行情。若产品要求页面必须复现 14:45 的完整排名，则该表应进入首版范围，但仍只存计算结果，不存原始行情。

## 8. 定时任务

### 8.1 调度入口

- 建议入口：`GET /api/cron/etf-rotation`；
- 仅接受 Vercel Cron 携带的服务端授权；未授权请求返回 401；
- 接口不在页面暴露，也不提供客户端调用入口；
- 运行时必须显式设置动态执行，禁止被框架静态缓存；
- 如使用 Vercel Cron 的 UTC 时区，配置 `45 6 * * *`。

### 8.2 运行状态机

```text
收到 Cron
→ 校验授权
→ 生成北京时间业务日期和幂等键
→ 已成功处理？返回已处理
→ 是否交易日？否：写 SKIPPED，发送跳过通知后结束
→ 初始化/读取唯一组合
→ 获取并校验 9 只 ETF 行情
→ 计算分数、稳定排序与目标 ETF
→ 数据库事务内更新交易、持仓、现金、快照与运行结果
→ 事务提交
→ 发送邮件并更新 email_status
```

稳定排序：得分降序；得分完全相同时按 ETF 池原始顺序排序，确保相同输入得到相同目标。

### 8.3 超时与重试

- 行情单次请求设置明确超时，禁止无限等待；
- 仅对网络超时、连接中断和上游 5xx 做有限重试，建议最多 2 次指数退避；
- 数据缺失、价格非法和计算结果非法不重试交易，只记录失败；
- 任务失败不得写交易、修改持仓或生成正常资产快照；
- Vercel 平台重试由幂等键兜底。

## 9. 邮件需求

### 9.1 成功邮件

主题建议：

```text
[ETF 动量轮动][HOLD] 2026-08-26 目标 518880
[ETF 动量轮动][SELL→BUY] 2026-08-26 513100 → 518880
```

正文至少包含：

1. 运行时间、行情时间和策略版本；
2. 本次动作：BUY、SELL→BUY 或 HOLD；
3. 买卖代码、价格、数量、金额和成交后现金；
4. 9 只 ETF 的本次动量排名与分数；
5. 当前持仓；
6. 总资产、可用资金、仓位占比、累计收益、年化收益和夏普比率；
7. “模拟组合，不构成投资建议”的声明。

### 9.2 失败邮件

主题：`[ETF 动量轮动][运行失败] YYYY-MM-DD`。

正文包含失败阶段、稳定错误码、脱敏错误摘要、持仓未改变说明和任务 ID。不得在邮件中包含数据库连接串、邮箱授权码、请求授权头或完整异常堆栈。

非交易日发送简短的跳过通知，主题建议为 `[ETF 动量轮动][休市跳过] YYYY-MM-DD`，正文说明当天不是交易日、组合未发生变化，并附最近一次成功运行时间。这样每天 14:45 的触发均有可见邮件结果。

### 9.3 密钥管理

发件地址、收件地址、SMTP 主机、端口和授权码全部使用 Vercel 服务端环境变量。代码、Git 历史、页面响应、运行日志和数据库均不得出现授权码明文。

## 10. 页面需求：ETF 动量轮动

### 10.1 页面定位

页面回答四个问题：

1. 今天的自动任务是否成功执行？
2. 当前持有什么、值多少钱、还有多少现金？
3. 组合从 100,000 元开始表现如何？
4. 最近发生了哪些买卖，固定 ETF 池当前是什么状态？

这是只读运行看板，不出现“运行策略”“买入”“卖出”“修改资金”等按钮。设计延续 Glacier Signal 的可追溯票据语言，但采用操作型信息层级：运行状态优先，其次是资产与持仓，再是曲线和交易记录。

### 10.2 导航

主导航新增“ETF 动量轮动”，建议顺序：

```text
今日选股 → 个股评分 → 仓位方案 → ETF 动量轮动
```

页面标题旁显示策略版本 `ETF 动量 1.0`，避免与现有“趋势回调转强”策略混淆。

### 10.3 首屏结构

首屏从上至下：

1. 页面标题“ETF 动量轮动”和一句说明“每天 14:45 自动计算，模拟持有固定 ETF 池中动量排名第 1 的标的”；
2. 运行状态条；
3. 关键资产指标；
4. 当前持仓主区域。

运行状态条包含：

- 最近一次任务状态：成功 / 失败 / 今日未运行 / 非交易日跳过；
- 最近成功估值时间；
- 行情数据时间；
- 当前目标 ETF 与本次动作；
- 下一次计划运行时间；
- 失败时展示简明原因及“持仓未改变”，不展示技术堆栈。

关键指标固定展示：

| 指标 | 主值 | 辅助信息 |
|---|---|---|
| 总资产 | 人民币金额 | 相对初始资金的金额变化 |
| 累计收益 | 百分比 | 开始日期 2026-06-08 |
| 年化收益 | 百分比 | 365 自然日口径 |
| 可用资金 | 人民币金额 | 占总资产比例 |
| 仓位占比 | 百分比 | 当前持仓市值 |
| 夏普比率 | 数值或 `—` | 数据不足时写明原因 |

颜色不能单独表达涨跌或任务状态，必须同时显示正负号、文字或图标。

### 10.4 当前持仓

有持仓时展示：

- ETF 名称与代码；
- 买入时间；
- 买入价；
- 等效持有份额；
- 初始投入金额；
- 最近估值价与估值时间；
- 当前市值；
- 浮动盈亏金额与收益率；
- 在总资产中的仓位占比。

空仓时显示“当前暂无持仓，系统将在下一个有效交易日 14:45 按策略买入排名第 1 的 ETF”，不得用全零假持仓填充。

### 10.5 收益率曲线

- 默认展示从 2026-06-08 至最近快照日的累计收益率曲线；
- X 轴为资产快照日期，Y 轴为累计收益率百分比；
- 基准线为 0%；
- 悬停或键盘聚焦显示日期、总资产、当日收益、累计收益和年化收益；
- 曲线下方可切换“累计收益率 / 总资产”，首屏默认累计收益率；
- 无数据时显示真实空状态，不生成演示曲线；
- 只有一个初始点时展示单点和“等待下一次有效运行后形成曲线”。

### 10.6 近 10 笔历史交易

按模拟成交时间倒序展示最近 10 条 BUY/SELL；HOLD 不属于交易，不进入此列表。

| 字段 | 展示要求 |
|---|---|
| 时间 | 北京时间，精确到分钟 |
| 动作 | 买入 / 卖出，文字与符号同时表达 |
| ETF | 名称与代码 |
| 成交价 | 人民币单价 |
| 数量 | 等效份额，明确为模拟口径 |
| 成交金额 | 人民币金额 |
| 实现盈亏 | 仅卖出显示；买入显示 `—` |
| 成交后资金 | 交易完成后的可用资金 |

若一次轮动先卖后买，两笔记录分别展示，通过相同任务时间与视觉连接表示属于同一次运行。无交易时显示“尚无交易记录”。

### 10.7 ETF 股票池列表

页面名称沿用用户习惯可写“ETF 股票池”，模块标题建议使用更准确的“ETF 池”。固定展示 9 个代码，不允许页面增删。

| 字段 | 展示要求 |
|---|---|
| 排名 | 若页面能获取本次临时排名则显示；否则显示 `—`，不得使用过期排名冒充当前排名 |
| ETF | 行情源返回的名称与固定代码；名称不可用时仅显示代码 |
| 最新价 | 页面请求时获取，附数据时间；获取失败显示 `—` |
| 动量得分 | 仅在使用同一批完整数据重新计算成功时显示 |
| 当前状态 | 当前持仓 / 本次目标 / ETF 池成员 |

固定代码：

```text
518880
513100
513350
159915
510180
159612
513030
159928
513520
```

ETF 名称从行情源动态获取，不在 PRD 或代码中凭记忆硬编码，避免基金更名后页面失真。

### 10.8 页面状态

| 状态 | 页面表现 |
|---|---|
| 正常 | 展示最近成功运行时间、账本、曲线、交易和 ETF 池 |
| 今日未运行 | 保留最近成功结果，提示今日 14:45 尚未到达或任务尚未完成 |
| 今日失败 | 保留最近成功结果，顶部警告“今日运行失败，持仓未改变” |
| 非交易日 | 显示“今日休市，策略未运行”，展示最近成功结果 |
| 首次初始化 | 展示 100,000 元现金、空仓、初始收益点和无交易空状态 |
| 行情列表失败 | 账本与历史正常展示；ETF 池实时字段显示不可用，不能影响已保存业务数据 |
| 数据库失败 | 页面显示服务暂不可用，不用 0 值或客户端缓存伪装正常数据 |

### 10.9 响应式与可访问性

- 桌面端：指标、持仓和运行状态构成首屏；曲线与交易列表在第二信息层；ETF 池在其后。
- 移动端：运行状态 → 总资产与收益 → 现金与仓位 → 当前持仓 → 曲线 → 交易 → ETF 池，全部单列，不允许横向滚动。
- 交易表和 ETF 池在窄屏转换为纵向记录卡，不强行压缩成不可读表格。
- 金额与百分比使用等宽数字；所有时间明确标注“北京时间”。
- 图表同时提供可读的数据摘要或可聚焦数据点，不以颜色作为唯一信息通道。
- 正收益、负收益、失败、成功均需文字标签；遵循系统减少动态效果设置。

## 11. 页面数据接口建议

为减少客户端拼接和口径漂移，页面由服务端聚合以下只读数据：

### 11.1 `GET /api/strategies/etf-rotation/overview`

返回：

- 最近运行状态与最近成功时间；
- 组合总资产、现金、持仓市值和仓位占比；
- 累计收益、年化收益、夏普比率；
- 当前持仓；
- 最近 10 笔交易；
- 全部资产快照或适合图表的有序序列；
- 固定 ETF 池及可选的即时行情状态。

接口约束：

- 只读，不产生交易或修改快照；
- 金额和高精度数值使用字符串传输，前端格式化显示；
- 返回 `asOf`、`marketDataAt` 和 `timezone`；
- 数据库结果可短时缓存，但今日失败状态不得被过期成功缓存覆盖；
- 页面实时行情失败时允许业务账本部分成功返回，并对实时字段给出明确错误状态。

## 12. 环境变量

至少需要：

```text
POSTGRES_URL
CRON_SECRET
SMTP_HOST
SMTP_PORT
SMTP_USER
SMTP_PASSWORD
EMAIL_FROM
EMAIL_TO
```

如行情数据源需要密钥，增加独立的服务端变量。除收件地址可在邮件头中出现外，其余敏感变量不得传到浏览器。

## 13. 验收标准

### 13.1 策略与数据

- [ ] 空数据库只初始化一个 `etf_momentum_v1` 组合，开始日期、100,000 元初始资金和初始快照与本 PRD 完全一致。
- [ ] 每次运行动态获取最近 24 个已完成交易日收盘价和当日 14:45 最新价，原始行情不入库。
- [ ] 9 只 ETF 均使用相同时间口径组成 25 个价格点。
- [ ] 给定相同 25 点输入，实现得分与原 Python/Numpy 公式在约定精度内一致。
- [ ] 得分为负时仍按最高分持有，不擅自加入空仓过滤。
- [ ] 分数相同时按固定 ETF 池原始顺序稳定排序。

### 13.2 定时任务与账本

- [ ] Vercel 每天北京时间 14:45 触发任务，非交易日安全跳过。
- [ ] 每次自然日触发后均发送对应的成功、失败或休市跳过邮件，且同一业务日期不重复发送正常结果。
- [ ] 页面没有任何手动执行、买卖或修改资金入口。
- [ ] 当前无持仓时全额模拟买入第 1 名；目标不变时 HOLD；目标变化时先卖后买。
- [ ] BUY/SELL、现金、持仓、快照和任务状态在一个数据库事务中保持一致。
- [ ] 同一业务日期并发或重复调用不会产生重复交易、重复快照或重复正常邮件。
- [ ] 任一 ETF 行情缺失、过期或非法时不改变持仓和现金。
- [ ] 邮件失败不会回滚已完成账本，任务日志可看到邮件失败状态。

### 13.3 指标与页面

- [ ] 页面显示总资产、累计收益、年化收益、可用资金、仓位占比和夏普比率。
- [ ] 指标按照第 6 节公式由同一批快照计算，页面与邮件值一致。
- [ ] 页面显示当前持仓全部规定字段；空仓时显示真实空状态。
- [ ] 页面显示累计收益率曲线，初始状态只有 2026-06-08 的 0% 单点。
- [ ] 页面按时间倒序显示最近 10 笔 BUY/SELL，HOLD 不占用交易名额。
- [ ] 页面固定展示 9 只 ETF；名称来自行情源，失败时不伪造名称或价格。
- [ ] 今日任务失败或未运行时继续展示上一次成功账本，并清楚标注数据时间与状态。
- [ ] 桌面、平板和手机均无横向页面滚动，状态不只依赖颜色表达。

### 13.4 安全

- [ ] Cron 接口拒绝未授权请求。
- [ ] SMTP 授权码、数据库连接串和 Cron 密钥只存在于服务端环境变量。
- [ ] 页面、接口响应、数据库错误字段、邮件和日志中没有敏感密钥。
- [ ] 部署和冷启动不会重置已有组合。

## 14. 测试场景

至少覆盖：

1. 空数据库首次初始化；
2. 空仓首次 BUY；
3. 排名不变 HOLD；
4. 排名变化 SELL→BUY；
5. 所有分数均为负但仍选择最高分；
6. 分数相同的稳定排序；
7. 单只 ETF 缺少第 24 个历史收盘价；
8. 实时价格缺失、为 0、负数、NaN 或过期；
9. 回归方差为 0；
10. 非交易日跳过；
11. 同一日期 Cron 并发调用；
12. 卖出与买入之间数据库事务失败；
13. 数据库提交成功但邮件失败；
14. 仅有一个快照时夏普比率为 `—`；
15. 交易少于 10 笔、等于 10 笔和超过 10 笔；
16. ETF 池实时行情失败但历史账本正常；
17. 手机端长金额、负收益和长错误文案不溢出。

## 15. 上线前必须处理

1. 立即废止并重新生成原脚本中曾以明文出现的邮箱授权码；旧授权码应视为已经泄露，不能继续使用。
2. 确认 Vercel 计划支持所需的每日 Cron 频率和函数最长执行时间。
3. 确认生产行情源在 14:45 能稳定返回 9 只 ETF 的同步实时快照与前复权历史数据。
4. 用固定样本建立 Python 原实现与新实现的公式对照测试。
5. 明确邮件服务在 Vercel 出站网络中的可用性；若 SMTP 端口受限，改用支持 HTTPS API 的邮件服务，但不改变本 PRD 的邮件内容和状态规则。

## 16. 后续版本候选

- 加入手续费、滑点和 100 份整数成交的真实交易口径，并与当前模拟净值分版本展示；
- 保存每日 9 只 ETF 的排名结果，以便页面回看任意一天的排名；
- 增加策略基准、最大回撤、波动率和月度收益；
- 增加邮件失败的自动重发与运维告警；
- 在明确权限模型后支持多用户和独立组合。
