---
title: Glacier Signal 全市场选股与回测系统 PRD
status: 待审阅
version: 1.1-draft
date: 2026-08-24
related:
  - "[[趋势回调转强评分页面-PRD]]"
  - "[[趋势回调转强策略优化-PRD-v0.5]]"
---

# Glacier Signal 全市场选股与回测系统 PRD

## 1. 产品摘要

| 项目 | 内容 |
|---|---|
| 产品定位 | 面向 A 股日线波段交易的“发现候选 → 解释评分 → 规划仓位 → 历史验证”工具 |
| 本期范围 | 全市场 TOP N 选股、复用当前评分、仓位规划、个股近两年回测 |
| 目标市场 | 首版沪深 A 股；北交所和 ETF 延后 |
| 数据方式 | 请求时从第三方接口获取，不在本地持久化行情、选股或回测结果 |
| 交易频率 | 交易日收盘后选股，T+1 开盘执行确认 |
| 策略基线 | 首版原样复用当前代码中的 0.4 评分与风险仓位口径 |
| 产品边界 | 不连接券商、不自动下单、不承诺收益；评分不等于上涨概率 |

> 版本说明：当前代码的模型版本为 0.4，仓库另有 0.5 策略实现草案。本 PRD 不把草案当作已上线规则。0.5 应先作为可选回测版本，验收后再决定是否替换默认策略。

## 2. 背景与问题

当前产品已经可以完成单只证券评估：用户输入代码，系统读取行情，执行硬性过滤、趋势/回调/转强评分，并根据账户风险计算止损和计划仓位。

当前链路存在四个断点：

1. 用户必须事先知道股票代码，系统不能主动发现候选。
2. 评分、仓位和历史表现不在同一条产品链路中。
3. 多只股票同时满足条件时，没有统一排序与组合风险分配。
4. 现有页面以单只股票的长表单为中心，不适合承载每日候选池。

## 3. 产品目标与非目标

### 3.1 核心目标

1. 一次扫描全市场，返回可解释、可复核的 TOP N 候选。
2. 选股、个股详情和回测使用同一套评分、硬性过滤、入场与退出规则。
3. 将“分数”和“可执行仓位”分开，高分不能覆盖止损、整手和组合风险限制。
4. 输出完整但克制的回测指标，覆盖收益、回撤、波动与交易质量。
5. 所有结果明确展示数据日期、数据源、复权方式、策略版本和未知项。

### 3.2 非目标

- 不做盘中实时扫描和分钟级交易。
- 不做自动下单、券商持仓同步、盈亏记账。
- 不保存账户参数、历史扫描、自选股或回测任务。
- 不引入新的基本面、AI 预测或主观评分因子。
- 不用今天存活的 TOP N 直接回放历史，避免幸存者偏差。

## 4. 核心用户流程

### 流程 A：收盘后发现候选

选择一套已注册策略 → 确认策略版本与固定参数 → 获取全市场数据 → 按该策略过滤、评分和排序 → 返回策略定义的 TOP N → 查看候选解释。

### 流程 B：生成下单前计划

勾选候选 → 输入账户净值与现有风险占用 → 系统按排名分配风险预算 → 输出各标的股数、仓位和最大计划亏损 → T+1 用实际开盘价复算。

### 流程 C：验证候选股票

从 TOP N 进入个股打分详情 → 点击“近两年回测” → 系统沿用该候选的策略版本和固定测试口径 → 重建该股票近两年的信号、买卖点与资金曲线 → 返回收益指标和交易明细。

## 5. 页面信息架构

| 一级页面 | 用户问题 | 主要输出 |
|---|---|---|
| 今日选股 | 今天哪些股票最符合当前策略？ | TOP N 候选列表 |
| 仓位方案 | 我的账户对这些候选各能买多少？ | 多标的计划仓位与组合风险 |
| 个股评分 | 某只股票为什么通过或失败？ | 当前评分、仓位入口、近两年回测入口 |

首页默认进入“今日选股”，不再以单只代码输入作为唯一主入口。代码搜索仍常驻顶部，用于快速进入个股评分。

首版不设置独立的全市场组合回测一级页面。回测从个股评分详情进入，服务于“这个 TOP N 候选在相同策略下，近两年出现过哪些买卖点、历史表现如何”的判断。全市场组合回测作为后续研究功能。

全站取消当前仅适合品牌落地页的居中窄胶囊导航，改为完整应用导航。首页也不再使用占满一屏的大型品牌 Hero 和装饰性评分预览，而是使用“紧凑产品说明 + 扫描控制 + 实际候选”的工作台结构。

## 6. 页面一：今日选股

### 6.1 首页定位

首页既是产品总入口，也是每日选股工作台。用户进入后应在 5 秒内理解：

1. 这是一个全市场选股系统，不只是单只股票评分器；
2. 当前使用哪个策略版本和哪个完整交易日；
3. 如何启动扫描；
4. 扫描完成后，哪里查看候选、仓位和回测。

首页的首要操作是“开始/重新扫描全市场”，单只股票代码搜索作为次要快捷入口。首屏不放与任务无关的品牌视觉资产，也不展示虚构的单股评分结果。

### 6.2 顶部菜单

顶部菜单采用完整应用栏，而不是悬浮在页面中央的窄胶囊。桌面端高度 64–72px，页面滚动时保持粘性；内容与 1280px 主网格对齐。

从左到右：

| 区域 | 内容 | 交互 |
|---|---|---|
| 品牌区 | Glacier Signal 标志与名称 | 点击返回“今日选股” |
| 主菜单 | 今日选股、个股评分、仓位方案 | 当前页面使用浅色实体背景或下划线标识 |
| 工具区 | 当前模型版本、股票代码搜索 | 搜索提交后进入个股评分 |

菜单要求：

- “今日选股”为首页和默认激活项；
- 菜单顺序与用户决策链路一致，不使用“功能”“关于”等营销型菜单占据主导航；
- 模型版本显示为“回调转强 0.4”，不可只显示难以理解的“0.4”；
- 股票搜索默认显示“搜索股票代码”，桌面端直接展开，窄屏可收起为搜索图标；
- 首版没有登录、消息、收藏和历史记录，不为未来功能预留空图标；
- 移动端显示 Logo、当前页面标题、搜索与菜单按钮；展开菜单后展示三个一级页面。

### 6.3 首页首屏

桌面端采用 7:5 左右布局，内容区顶部距离导航 40–56px。

左侧：

- 小标签：全市场 · 已注册策略扫描；
- 主标题：选择一套策略，验证今天的市场信号；
- 副标题：说明系统严格按选中策略的固定版本执行，不在扫描时临时调整参数；
- 不放股票代码大输入框，不使用“开始评估”作为首要按钮。

右侧为策略选择与扫描卡，直接承载：

- 数据日期与“完整交易日”状态；
- 已注册策略选择器；
- 策略名称、版本和“当前 / 研究”状态；
- 只读策略摘要：适用证券、市场范围、流动性门槛、评分门槛和 TOP N；
- 主按钮“使用此策略扫描”或“使用此策略重新扫描”；
- “结果不在本地保存”的简短提示。

扫描卡不提供市场范围、流动性、评分阈值或 TOP N 的临时编辑控件。需要验证另一组参数时，应新增一个有独立版本号的研究策略，再从策略选择器中运行。

首屏目标是在常见 1440×900 桌面屏幕中同时露出“今日候选”的标题和扫描摘要，让用户明确扫描结果就在本页下方，而不是进入另一个营销漏斗。

### 6.4 首页扫描状态切换

首页保持同一布局骨架，根据任务状态替换内容：

| 状态 | 首屏表现 | 结果区表现 |
|---|---|---|
| 首次进入 | 扫描按钮为主操作 | 展示选股流程和数据要求，不放假数据 |
| 扫描中 | 控制项锁定，按钮变为当前阶段 | 展示已处理数量、阶段和取消操作 |
| 扫描完成 | 显示生成时间和“重新扫描” | 直接展示摘要与 TOP N |
| 部分失败 | 扫描卡显示完整度警告 | 超过门槛时不输出正常候选榜 |
| 无候选 | 扫描成功但候选为 0 | 展示排除原因分布，不建议降低门槛 |
| 任务过期 | 保留上次参数 | 提示结果未保存并允许重新运行 |

扫描完成后不跳转新页面，首页原地进入结果态。这样顶部参数、数据口径和候选列表始终保持上下文连续。

### 6.5 首页结果区布局

结果区从上至下：

1. 标题“今日候选”和一行规则说明；
2. 扫描摘要：扫描总数、完成评分、可执行候选、数据失败数；
3. 状态切换：候选、观察、排除原因；
4. TOP N 主列表；
5. 数据源、策略版本和风险声明。

不在候选列表上方堆叠多个大型 KPI 卡。扫描数量是上下文，不是需要视觉庆祝的业绩指标，使用紧凑的行内摘要即可。

### 6.6 响应式要求

- 桌面端：完整菜单、两栏首屏、候选表格；
- 平板端：首屏改为上下两块，候选表格隐藏低优先级列；
- 手机端：顶部使用折叠菜单，扫描参数为两列或单列，候选表格转换为卡片；
- 手机候选卡首屏必须看到排名、股票、总分、状态和止损风险；
- 成交额、完整评分明细和操作按钮可进入卡片详情，不使用横向滚动；
- 移动端底部可增加“今日选股 / 仓位 / 回测”三个主要入口，但不能与展开菜单重复占用过多空间。

### 6.7 策略选择与扫描控制

| 字段 | 默认值 | 规则 |
|---|---:|---|
| 扫描日期 | 最新完整交易日 | 不使用未完成的当日日线；历史扫描可在测试入口选择日期 |
| 扫描策略 | 趋势回调转强 · 股票 0.4 | 只能选择已注册策略 |
| 策略状态 | 当前 | 当前 / 研究；研究策略始终带醒目标识 |
| 适用市场 | 沪深 A 股 | 来自策略配置，只读 |
| 股票池规则 | 上市天数、交易状态、流动性等 | 来自策略配置，只读 |
| 排名与输出 | 排序规则、TOP N | 来自策略配置，只读 |
| 参数版本 | 策略参数快照版本 | 来自策略配置，只读 |

主按钮为“使用此策略扫描全市场”。运行中显示真实阶段：加载策略 → 获取证券池 → 基础过滤 → 读取日线 → 评分排名，不显示虚假的精确进度。

策略选择器首版建议包含：

| 策略 | 状态 | 页面行为 |
|---|---|---|
| 趋势回调转强 · 股票 0.4 | 当前 | 默认选中，可生成候选和仓位 |
| 趋势回调转强 · 股票 0.5 | 研究 | 可扫描和回测，结果标记“研究”，是否生成仓位由策略状态决定 |
| 强趋势突破 · 股票 0.1 | 研究 | 可扫描和回测，默认不输出可执行仓位 |
| 权益 ETF 0.4E | 未启用 | 未满足数据要求前不出现在普通选择器中 |

### 6.8 扫描摘要

完成后显示：

- 数据截止日期、复权方式和第三方数据源；
- 市场证券总数、基础过滤后数量、完成评分数量、可执行候选数量；
- 数据缺失或读取失败的证券数量；
- 扫描耗时和结果生成时间。

候选少于 N 时只返回实际数量，不用低质量股票补足。

### 6.9 TOP N 列表

| 字段 | 展示要求 |
|---|---|
| 排名 | 1 至 N，规则稳定且可解释 |
| 股票 | 名称、代码、市场 |
| 当前结论 | 候选 / 等待转强 / 临界突破观察；默认榜单只显示候选 |
| 总分 | 0–100，明确说明不是胜率 |
| 技术分 | 0–90 |
| 转强分 | 0–35 |
| 风险标签 | 止损距离、风险系数、压力空间状态 |
| 流动性 | 20 日平均成交额 |
| 信号日 | 使用的完整交易日 |
| 操作 | 查看详情、加入仓位方案 |

默认排序规则：

~~~text
所有硬性条件可执行
→ 总分降序
→ 转强分降序
→ 止损距离升序
→ 20日平均成交额降序
→ 股票代码升序
~~~

最后用股票代码确保相同输入得到稳定顺序。用户可以切换“候选”“观察”“已排除”。已排除列表以首个拒绝原因为主，不一次渲染数千只股票的全部评分明细。

### 6.10 候选详情抽屉

点击候选后在当前页快速展示：

- 评分模块和硬性过滤摘要；
- 计划入场价、初始止损、止损距离与 2R 空间；
- 进入当前排名的原因；
- T+1 取消条件；
- “完整个股评分”和“加入仓位方案”两个操作。

## 7. 全市场选股规则

### 7.1 扫描管线

~~~text
载入所选策略及不可变参数快照
→
第三方全市场证券列表
→ 按策略定义执行证券池过滤
→ 获取至少250日前复权 OHLCV
→ 执行该策略的硬性过滤与评分
→ 执行该策略的入场、止损与压力空间规则
→ 按该策略的排名规则稳定排序
→ 返回该策略定义的 TOP N
~~~

关键字段缺失时不得默认通过。缺失影响硬性条件时排除；仅影响非硬性展示项时标记“数据不完整”，且不进入默认 TOP N。

### 7.2 当前策略复用

首版直接复用当前已经实现的口径：

- 趋势质量 30 分；
- 回调质量 25 分；
- 重新转强 35 分；
- 市场环境 10 分；
- 总分至少 70，技术分至少 65/90，转强分至少 21/35；
- 硬性过滤失败时，不得因总分高而进入可执行候选。

全市场页面只批量调用策略注册表中的同一评分引擎，不建立第二套“选股分”。当前 0.4 的市场范围、股票池、评分、排序、TOP N、仓位和退出参数必须作为一个完整配置载入。

### 7.3 策略注册表

系统通过策略注册表管理可扫描和可回测的策略。策略不是一组页面临时参数，而是一个具有明确身份和版本的产品对象。

每个策略至少包含：

| 字段 | 说明 |
|---|---|
| strategyId | 稳定标识，例如 pullback-strength-stock |
| strategyVersion | 策略语义版本，例如 0.4、0.5 |
| parameterVersion | 完整参数快照版本 |
| displayName | 页面显示名称 |
| status | current / research / disabled |
| instrumentProfile | 普通股票、权益 ETF 等证券类型 |
| universeConfig | 市场、上市天数、交易状态、流动性门槛 |
| signalConfig | 指标、评分、硬性过滤和触发条件 |
| rankingConfig | 候选排序与稳定的同分规则 |
| outputConfig | TOP N 和候选/观察输出范围 |
| positionConfig | 风险档位、止损系数和组合上限 |
| exitConfig | 止损、趋势退出和时间退出 |

策略管理规则：

1. 已发布版本不可直接修改；任何参数变化必须生成新版本。
2. current 策略可用于选股、仓位和回测。
3. research 策略可用于扫描和回测，页面始终标记“研究”；是否允许生成仓位由策略配置决定。
4. disabled 策略不出现在普通策略选择器中，但保留历史回归能力。
5. 新增策略通过代码配置、自动测试和发布流程注册，不在扫描页面提供“创建策略”或参数编辑器。
6. 每次扫描和回测结果都携带 strategyId、strategyVersion、parameterVersion 和参数摘要。
7. 同一策略版本在今日扫描、历史扫描、个股详情和回测中必须调用同一规则实现。

## 8. 页面二：仓位方案

### 8.1 功能边界

本期的“仓位管理”指下单前的计划仓位和组合容量管理，不是持仓记账。所有输入仅对当前会话有效，刷新后不恢复。

### 8.2 输入

- 从 TOP N 勾选的候选；
- 账户净值；
- 当前全部未平仓初始风险金额；
- 每只候选已有持仓市值；
- 候选所属行业及当前行业持仓市值；
- 是否连续 3 笔完整止损；
- 计划入场价，T+1 可改为实际开盘价。

### 8.3 计算口径

~~~text
基础风险预算 = 账户净值 × 评分档位风险比例
调整后预算 = 基础风险预算 × 止损距离系数 × 连续止损系数
可用风险预算 = min(调整后预算, 组合剩余风险) × 90%
理论股数 = 可用风险预算 / (入场价 - 初始止损价)
最终股数 = 理论股数向下取整至100股后，再受单股、行业和组合上限约束
~~~

沿用当前上限：单股市值不超过净值 15%，单行业不超过 30%，全部未平仓初始风险不超过 2%，保留 10% 风险缓冲。

多候选默认按 TOP N 排名从高到低依次占用额度。页面提示“分配顺序会影响结果”，允许拖动优先级后重新计算。

### 8.4 输出

组合摘要：

- 计划总市值和总仓位比例；
- 新增计划风险；
- 加上已有持仓后的总初始风险；
- 剩余风险额度。

每只候选：

- 排名、入场价、止损价、止损距离、风险系数；
- 股数、计划市值、计划亏损、仓位比例；
- 限制仓位的第一原因。

不足 100 股时输出 0 股和“整手约束”，不自动缩短止损。

## 9. 个股详情内的近两年回测

### 9.1 入口与上下文

从首页 TOP N 点击股票后进入打分详情。详情页顶部结果区提供两个后续操作：

1. 加入仓位方案；
2. 近两年回测。

回测入口继承首页候选的 strategyId、strategyVersion、parameterVersion、股票代码和信号日期，用户不需要再次选择策略。

如果用户通过股票代码搜索直接进入详情，没有首页上下文，则使用当前 current 策略，并在按钮旁明确显示策略名称和版本。

回测建议使用独立子页面或详情页内的独立标签，避免把图表和交易明细直接堆在评分长页面中。返回详情后保留股票和策略上下文。

### 9.2 时间与数据口径

| 项目 | 固定口径 |
|---|---|
| 回测结束日 | 当前详情使用的数据截止日或信号日 |
| 回测开始日 | 结束日前推 2 个自然年后的首个交易日 |
| 指标预热 | 额外获取开始日前至少 250 个交易日，只计算指标，不计入收益 |
| 行情 | 前复权日线 OHLCV |
| 策略 | 继承详情页的已注册策略版本 |
| 初始资金 | 100,000 元测试账户 |
| 买卖佣金 | 单边 0.03%，最低 5 元 |
| 印花税 | 按卖出日适用税率；数据不足时明确统一假设 |
| 滑点 | 单边 0.10% |
| Sharpe 无风险利率 | 0% |

首版不提供日期、初始资金、成本、评分、仓位或退出参数编辑。改变测试条件时应建立新的测试场景或策略版本，保证结果可复现。

页面必须显示实际覆盖的首尾交易日、有效交易日数、数据源、复权方式、策略版本和成本假设。

### 9.3 信号与成交规则

- 对该股票的每个历史交易日执行与详情页相同的硬性过滤和评分；
- T 日评分只使用 T 日收盘前已知数据；
- 满足策略条件后在 T+1 开盘模拟入场；
- T+1 高开超过策略限制时取消该信号，并记录取消原因；
- 持仓期间不重复开仓；
- 涨跌停、停牌或无法成交时不得假设成交；
- 跳空越过止损时使用实际开盘价及滑点；
- 盘中最低价触及事先确定的止损时，按策略定义的止损成交模型执行；
- 收盘产生的退出条件只能在下一交易日成交；
- 仓位使用该策略的固定风险规则，股数向下取整到 100 股；
- 期末未退出持仓标记为“期末结算”。

### 9.4 买卖点图

回测结果首先展示该股票近两年的价格与买卖数据点：

- 主图使用日 K 或收盘价格线，桌面端默认日 K，移动端可简化为收盘线；
- 买入点使用向上标记，显示入场日期和成交价；
- 卖出点使用向下标记，显示退出日期、成交价和退出原因；
- 买卖点同时使用图形、文字和方向区分，不能只依赖红绿色；
- 悬停或点击买入点时，显示信号分数、初始止损、股数和计划风险；
- 悬停或点击卖出点时，显示持仓天数、净盈亏、收益率、R 收益和退出原因；
- 同一笔交易的买卖点可用低对比虚线关联；
- 图表下方可切换策略净值和回撤，不把多个坐标轴叠在同一主图。

没有交易时仍展示完整价格走势，并说明历史信号的首要拒绝条件，不显示空白图表。

### 9.5 核心结果指标

| 指标 | 统一定义 |
|---|---|
| 累计收益率 | (期末策略账户净值 / 初始资金) - 1 |
| 年化收益率 | (期末净值 / 初始净值) ^ (252 / 交易日数) - 1 |
| 最大回撤 | 策略账户净值相对此前历史高点的最大跌幅，并展示起止日期 |
| Sharpe | (日收益均值 - 日化无风险利率) / 日收益标准差 × √252 |
| Calmar | 年化收益率 / 最大回撤绝对值 |
| 胜率 | 已平仓盈利交易数 / 已平仓交易数 |
| 盈亏比 | 盈利交易平均净利润 / 亏损交易平均净亏损绝对值 |
| 交易次数 | 已平仓交易数；期末未平仓另列 |
| 平均持仓天数 | 已平仓交易平均持有交易日数 |
| 收益波动率 | 策略账户日收益率样本标准差 × √252 |
| 最大连续亏损次数 | 按平仓时间排序后，连续净亏损的最大笔数 |

补充展示盈利因子、平均 R、平均资金使用率和该股票同期买入持有收益。买入持有仅作为参照，不与使用风险仓位的策略收益混为同一口径。

特殊情况：

- 无亏损交易时，盈亏比显示“—”，不显示无穷大；
- 最大回撤为 0 时，Calmar 显示“—”；
- 日收益波动为 0 时，Sharpe 显示“—”；
- 两年内无交易时，交易型指标显示“—”；
- 交易少于 10 笔时显示“样本不足，结果仅用于检查规则执行”，不得用高胜率描述策略质量。

### 9.6 交易明细

每笔交易至少显示：

| 字段 | 说明 |
|---|---|
| 序号 | 按入场时间升序 |
| 信号日 | 产生候选评分的 T 日 |
| 信号分数 | 总分、技术分、转强分 |
| 买入 | T+1 日期、成交价、股数 |
| 初始止损 | 止损价、止损距离、计划风险 |
| 卖出 | 日期、成交价、退出原因 |
| 结果 | 净盈亏、收益率、R 收益 |
| 持仓天数 | 持有交易日数量 |

点击交易明细时，价格图定位并高亮对应的买卖点。

### 9.7 结果页布局

1. 股票与口径栏：股票、两年区间、策略版本、数据源、复权和成本。
2. 买卖点主图：近两年价格、买入和卖出标记。
3. 核心指标：年化收益、最大回撤、Sharpe、Calmar 优先展示，其余指标紧随其后。
4. 策略净值与回撤：共用时间轴，上方净值、下方回撤。
5. 交易明细：与买卖点图联动。
6. 规则执行摘要：信号数量、成功入场、被取消信号和首要拒绝原因。

## 10. 个股评分页调整

现有页面保留为深度详情页，本次不重写评分交互，只做以下串联：

- 加入全站导航和“返回今日选股”；
- 显示完整策略名称及版本；
- 顶部同时显示候选排名、信号日和数据源；
- 顶部同时提供“加入仓位方案”和“近两年回测”；
- 回测入口继承当前股票、策略版本、参数版本和信号日期；
- 选股、详情和回测调用同一规则模块，相同证券及日期的结果必须一致。

## 11. 数据与第三方接口要求

### 11.1 必要数据

- 当日与历史证券列表：代码、名称、市场、上市日、ST/退市/停牌状态；
- 至少 250 个交易日的前复权 OHLCV 和成交额；
- 交易日历、涨跌停限制和可交易状态；
- 中证全指日线，用于当前市场环境 10 分；
- 行业分类，用于仓位上限；
- 历史退市、ST 变更和停牌记录，用于降低回测幸存者偏差。

### 11.2 不本地存储原则

1. 服务端不把行情、候选列表、账户输入、交易明细或回测结果写入本地文件或业务数据库。
2. 可使用请求内存和有明确过期时间的进程内短缓存，避免单次扫描重复请求；进程重启后可丢失。
3. 如使用第三方托管任务，页面必须说明结果有效期和数据去向。
4. 页面刷新、关闭或任务过期后，用户需要重新运行。
5. 首版不提供“最近扫描”“历史回测任务”或持久自选股。

### 11.3 数据源策略

- 全市场选股必须使用支持批量数据的主数据源；现有东方财富/腾讯单股请求继续作为详情页回退。
- 开发前验证授权条款、调用频率、批量接口、历史长度、复权、历史证券状态和稳定性。
- 限流、超时、字段变化和单只数据异常必须可识别，不能静默返回不完整 TOP N。
- 关键指数或超过 5% 的基础证券数据失败时，本次结果标记“不完整”，默认不输出正常 TOP N 结论。

## 12. 接口草案

| 接口 | 用途 | 说明 |
|---|---|---|
| POST /api/screenings | 按已注册策略启动全市场扫描 | 必须提交 strategyId、strategyVersion 和扫描日期；返回临时 jobId |
| GET /api/screenings/{jobId} | 查询临时进度与结果 | jobId 过期后返回 410 |
| GET /api/market-data/{symbol} | 获取单股评分数据 | 沿用现有能力 |
| POST /api/positions/preview | 计算仓位方案 | 无状态，不保存 |
| POST /api/backtests/stock | 执行个股近两年回测 | 提交 symbol、策略身份、结束日和固定 testProfile；同步返回指标、买卖点、净值和交易明细，不保存 |

全市场扫描临时任务建议有效 30–60 分钟。个股两年回测的数据量有限，首版使用同步请求；若达到超时阈值则明确失败并允许重试，不创建本地持久任务。

## 13. 关键状态与降级

| 状态 | 页面处理 |
|---|---|
| 未开始 | 展示参数和“结果不保存”提示 |
| 运行中 | 展示阶段、已处理数量和取消操作 |
| 部分失败 | 展示失败数量及是否超过完整性门槛 |
| 完全失败 | 保留参数，说明数据源/限流/网络/参数原因并允许重试 |
| 无候选 | 显示淘汰原因分布，不建议降低门槛 |
| 任务过期 | 告知结果未保存，可用原参数重新运行 |

全市场数据源不可用时，单股评分可继续使用现有回退源；不得用数千个单股串行请求伪装成完整的全市场扫描。

## 14. 非功能要求

- 一致性：相同证券、日期、行情快照和策略版本在三个模块中的结果一致。
- 可解释：每个候选保留通过项、失败项、原始指标和排序键。
- 可复现：展示数据截止日、复权、数据源和参数摘要；上游修订导致无法复现时明确说明。
- 性能目标：单股评分 P95 5 秒内；批量接口正常时日度扫描目标 60 秒内；个股近两年回测目标 10 秒内返回。
- 响应式：移动端用候选卡片，不依赖横向滚动才能理解核心结论。
- 安全：第三方 API 密钥仅在服务端，错误信息不得暴露密钥或完整上游请求。

## 15. 验收标准

### 15.1 选股

- [ ] 用户可从已注册策略中选择一个版本，按该版本的固定市场、股票池、排名和 TOP N 扫描。
- [ ] 扫描页面不能临时修改流动性、评分阈值、TOP N 或其他策略参数。
- [ ] 当前策略与研究策略视觉上明确区分，研究策略不会误显示为正式执行策略。
- [ ] 少于 N 只合格股票时不补足，无候选时不降低规则。
- [ ] 候选、观察和排除状态分开，每只候选可查看排名原因。
- [ ] 扫描页与个股页的评分、止损和结论一致。
- [ ] 数据源、日期、复权和策略版本可见。

### 15.2 仓位

- [ ] 单股和多候选通过同一仓位函数计算。
- [ ] 风险档位、止损系数、连续止损、10% 缓冲和组合上限按规定顺序生效。
- [ ] 调整候选优先级后，后续标的的可用风险额度正确重算。
- [ ] 不突破单股 15%、行业 30% 和总初始风险 2% 上限。
- [ ] 结果不写入本地持久存储。

### 15.3 回测

- [ ] 个股详情顶部存在“近两年回测”入口，并继承股票代码及完整策略身份。
- [ ] 回测覆盖结束日前两年，并额外获取足够的指标预热数据。
- [ ] 价格图展示全部实际买入和卖出点，买卖点可查看日期、价格、仓位、收益和退出原因。
- [ ] 输出年化收益率、最大回撤、Sharpe、Calmar、胜率、盈亏比、交易次数、平均持仓天数、收益波动率和最大连续亏损次数。
- [ ] 指标、图表和交易明细使用同一净值序列与交易记录。
- [ ] T 日信号不使用未来数据，收盘退出信号不在当日收盘成交。
- [ ] 佣金、印花税和滑点进入净值与单笔盈亏。
- [ ] 特殊样本不返回 NaN 或误导性无穷大。
- [ ] 相同输入与策略参数重复运行得到一致结果。

## 16. 分期建议

### Phase 0：共享规则基座

- 将指标、评分、硬性过滤、仓位和退出拆成共享模块；
- 锁定当前 0.4 行为并补充回归测试；
- 建立策略注册表，定义策略身份、状态、版本和不可变参数快照。

### Phase 1：选股与个股串联

- 完成批量数据源验证；
- 完成今日选股、TOP N 和候选抽屉；
- 从候选进入现有个股评分。

### Phase 2：多候选仓位方案

- 完成无状态组合仓位计算；
- 支持候选优先级调整和风险占用重算；
- 完成 T+1 价格复算与取消条件。

### Phase 3：回测

- 完成详情页“近两年回测”入口；
- 完成单证券规则回放与固定案例回归；
- 输出买卖点、指标、净值/回撤曲线、交易明细和数据完整性报告。

### Phase 4：策略版本对比

- 将 0.5 和 ETF 策略作为研究配置接入；
- 支持同一股票、同两年区间、同成本下的版本对比；
- 将全市场组合回测作为独立后续研究功能评估；
- 达到研究门槛后再决定默认版本。

## 17. 待确认决策

1. 首版全市场是否只覆盖沪深 A 股，北交所作为后续开关？
2. 当前 0.4 策略固定返回的 TOP N 是否设为 20？其他数量应作为新的测试场景版本，而不是扫描页临时参数。
3. 研究策略是否允许输出模拟仓位，还是只能显示扫描和回测结果？
4. “仓位管理”首版是否确认为下单前计划，不包括真实持仓记账？
5. 是否允许 30–60 分钟的服务器内存或第三方临时任务？若完全不允许，页面关闭后不能继续长回测。
6. 当前 0.4 与草案 0.5 的路线：0.4 标记 current，0.5 标记 research，是否符合预期？

## 18. 建议决策

1. 先以当前 0.4 作为唯一默认执行策略，页面重构时不改交易参数。
2. 当前 0.4 的 outputConfig 固定为 TOP 20；需要测试 TOP 10 或 TOP 50 时生成独立测试版本。
3. 扫描页只选择策略，不编辑策略参数；新增策略通过注册表、测试和发布流程接入。
4. 研究策略可以扫描和回测，但默认不输出可执行仓位。
5. 首版覆盖沪深 A 股；北交所和权益 ETF 通过独立策略加入，而不是扫描页市场开关。
6. 仓位管理首版只做计划，不记账、不连券商。
7. 首版只做详情页内的个股近两年回测；全市场组合回测后续独立规划。
8. 允许进程内临时缓存和短期任务结果，默认 60 分钟后失效，不作为业务记录。
