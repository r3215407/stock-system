---
title: Glacier Signal 个股评分与持仓管理增强 PRD
status: 待评审
version: 1.0-draft
date: 2026-08-28
related:
  - "[[全市场选股与回测系统-PRD-v1.0]]"
---

# Glacier Signal 个股评分与持仓管理增强 PRD

## 1. 产品摘要

| 项目 | 内容 |
|---|---|
| 背景 | 当前产品已经能完成个股评分、全市场候选筛选和计划仓位计算，但评分规则缺少集中、准确的说明；仓位页只服务买入前，无法回答已买入且盈利时如何管理；个股评分页也缺少直接加入仓位方案的入口。 |
| 本期目标 | 让用户看懂“为什么得到这个分、为什么仍不可交易”；让计划仓位自然转为已持有仓位并显示止盈/退出策略；打通个股评分到仓位方案的操作链路。 |
| 当前策略基线 | 趋势回调转强 · 股票 0.4，参数版本 `0.4-current`。 |
| 数据口径 | 日线、前复权；评分默认使用最新完整交易日，15:05 前不使用当日未完成日线。 |
| 存储边界 | 首版不登录、不连接券商。系统使用 PostgreSQL 保存一份全局共享的默认模拟盘；所有浏览器和设备读取、修改同一份仓位方案，刷新或重新打开后数据保持一致。 |
| 风险边界 | 评分表示与模型的匹配程度，不是上涨概率；止盈策略是规则提示，不自动下单。 |

## 2. 问题定义

### 2.1 评分信息分散

评分页能展开各模块，但用户仍需自行拼接总分、技术门槛、硬性过滤、重新转强和仓位规则。高总分但不可执行时，容易被理解为系统结论矛盾。

### 2.2 仓位页停留在买入前

当前仓位方案只接受计划入场价、已有该股市值和已有行业市值，输出计划股数、计划亏损与仓位。用户实际买入后，页面不知道买入成本、实际股数和持有天数，无法展示当前盈利、R 倍数、保护止损以及下一项退出条件。

### 2.3 个股评分与仓位方案未闭环

“今日选股”候选详情已有“加入仓位方案”，但个股评分页只有回测入口。通过股票代码直接评分的用户无法把当前结果加入仓位草案。

## 3. 目标与非目标

### 3.1 产品目标

1. 提供一页与当前代码行为一致、可按版本追溯的评分规则说明。
2. 明确区分“评分”“准入结论”“仓位大小”三个不同结果。
3. 在仓位方案中支持计划买入和已持有两种状态。
4. 已持有且盈利时，按 0.4 现有退出规则显示当前阶段、保护价和下一步策略。
5. 支持从个股评分页一键追加标的，并避免重复和上下文丢失。

### 3.2 非目标

- 不连接券商，不读取真实成交或自动卖出。
- 不计算税务成本，不做分红、送转、拆合股的持仓账务处理。
- 不支持多次买入批次、部分卖出、做 T 或多账户合并；首版只维护单一平均成本和总股数。
- 不新增主观目标价，不承诺固定收益。
- 不在本期修改 0.4 的评分、仓位或退出参数；规则变化必须发布新策略版本。
- 不做真实账户、用户隔离、权限控制或多模拟盘切换；首版所有访问者共用一份模拟盘数据。

## 4. 当前评分逻辑说明（As-Is）

本节是功能 1 页面必须准确呈现的当前行为，也是本 PRD 的验收基线。

### 4.1 总分结构

```text
技术分 = 个股趋势质量（30）+ 回调质量（25）+ 重新转强信号（35）= 90分
总分 = 技术分（90）+ 市场环境（10）= 100分
```

市场环境数据不可用时，总分只判定 90 分，剩余 10 分显示“待判定”，不得按 0 分冒充完整结果。

### 4.2 个股趋势质量：30 分

每项通过得 6 分，否则 0 分。

| 条件 | 分值 | 当前判定 |
|---|---:|---|
| 收盘价高于 MA20 | 6 | `收盘 > MA20` |
| MA5 高于 MA10 | 6 | `MA5 > MA10` |
| MA10 高于 MA20 | 6 | `MA10 > MA20` |
| MA20 五日斜率不少于 1% | 6 | `当日MA20 / 5日前MA20 - 1 >= 1%` |
| 20 日收益为正且不超过 20% | 6 | `0 < 当日收盘 / 20日前收盘 - 1 <= 20%` |

### 4.3 回调质量：25 分

回调观察窗口为最近 10 个交易日。回调低点按窗口内最低收盘计算；结构止损另使用窗口内最低价。

| 条件 | 分值 | 当前判定 |
|---|---:|---|
| 最长回落或横盘 | 6 | 最近 10 日内，连续至少 2 日满足“当日收益不大于 0，或绝对涨跌不超过 0.5%” |
| 回调幅度 | 7 / 5 / 3 / 0 | `[3%, 6%)` 得 7；`[2%, 3%)` 或 `[6%, 8%)` 得 5；`[8%, 10%)` 得 3；其他得 0 |
| 回调低点距 MA20 | 6 / 3 / 0 | 不超过 3% 得 6；大于 3% 且不超过 5% 得 3；其他得 0 |
| 回调与 MA60 关系 | 6 / 3 / 0 | 10 日内所有收盘均不低于 MA60 得 6；最低比例不低于 MA60 的 98% 得 3；其他得 0 |

边界值必须按上表实现。例如回调恰好 6% 得 5 分，恰好 10% 得 0 分；低点距 MA20 恰好 3% 得 6 分。

### 4.4 重新转强信号：35 分

| 条件 | 分值 | 当前判定 |
|---|---:|---|
| 阳线 | 5 | `收盘 > 开盘` |
| 高于前收盘 | 5 | `当日收盘 > 前一日收盘` |
| 重新站上 MA5 | 8 | 当日收盘高于当日 MA5，且此前 3 日中至少 1 日收盘不高于对应日 MA5 |
| 突破前三日最高收盘 | 9 | 当日收盘高于此前 3 日最高收盘 |
| MACD 柱改善 | 8 | MACD(12,26,9) 柱连续两次上升，或前一日为负且当日转正 |

“重新转强触发器”不是五项任意一项，而是“重新站上 MA5”或“突破前三日最高收盘”至少成立一项。

### 4.5 市场环境：10 分

基准为中证全指 `000985.SH`。

| 条件 | 分值 | 当前判定 |
|---|---:|---|
| 基准收盘高于 MA20 | 4 | `收盘 > MA20` |
| 基准 MA20 向上 | 4 | `当日MA20 > 5日前MA20` |
| 基准 5 日收益为正 | 2 | `5日收益 > 0` |

### 4.6 硬性过滤与人工确认

硬性条件失败不能用高分抵消。

| 编号 | 条件 | 当前规则 |
|---|---|---|
| HF-02 | 收盘价高于 MA60 | 必须严格高于 |
| HF-03 | MA20 高于 MA60 | 必须严格高于 |
| HF-04 | MA20 最近 5 日向上 | 当日 MA20 必须高于 5 日前 MA20 |
| HF-05 | 重新转强触发 | 重新站上 MA5 或突破前三日最高收盘；未触发时进入等待，不由高分替代 |
| HF-06 | 结构止损距离有效 | 每股风险大于 0，且止损距离不超过 12% |
| HF-07 | 压力空间有效 | 已突破此前 60 日最高收盘，或入场到该压力位的空间不少于 2R |
| HF-08 | T+1 高开不超过 2% | 有次日数据时判定；没有次日数据时保持待确认 |
| 人工确认 1 | 未来 5 个交易日事件风险 | 存在财报、解禁或重大事件则作废；不确定则待确认 |
| 人工确认 2 | 交易动机 | “担心错过或急于回本”则作废；不确定则待确认 |

结构止损价为：

```text
初始止损 = 最近10日最低价 - 0.3 × ATR(14)
每股初始风险 R = 入场价 - 初始止损价
止损距离 = 每股初始风险 / 入场价
```

价格小于 10 元时按 0.001 元向下取整，否则按 0.01 元向下取整。全市场扫描当前统一按 0.01 元向下取整；这是一项已知口径差异，实施本期功能前应统一，以保证同股同日结果一致。

### 4.7 准入结论与仓位档位

可执行候选至少满足：总分不低于 70、技术分不低于 65、转强分不低于 21、重新转强已触发、硬性过滤通过、人工确认完成且通过、止损档位可执行。

| 总分 | 页面结论 | 基础单笔风险预算 |
|---:|---|---:|
| 90–100 | 高匹配 | 账户净值的 0.75% |
| 80–89 | 标准交易 | 账户净值的 0.50% |
| 70–79 | 允许试仓 | 账户净值的 0.25% |
| 0–69 | 不交易 | 0 |

止损距离继续调整风险预算：`(0, 8%]` 为 1 倍；`(8%, 10%]` 为 0.5 倍；`(10%, 12%]` 为 0.25 倍且总分必须不低于 80；超过 12% 不执行。最近连续 3 笔完整止损时再乘 0.5。

组合仓位还受以下约束：组合初始风险不超过账户净值 2%，单股市值不超过 15%，行业市值不超过 30%，风险预算保留 10% 缓冲，股数向下取整到 100 股。因此“评分合格”不等于“一定能买到一手”。

## 5. 功能 1：评分规则说明页

### 5.1 页面定位

新增独立页面“评分规则”，建议路由 `/methodology/stock-score`。入口位于：

- 个股评分页“模型 0.4 参数依据”附近的“查看完整规则”；
- 顶部模型版本菜单中的“评分规则”；
- 评分模块标题旁的帮助图标。

不把它做成营销页，也不替代个股评分详情。页面回答“系统如何算、哪些条件会否决、分数如何影响仓位”。

### 5.2 页面内容

1. 顶部固定显示策略全名、策略版本、参数版本和适用范围。
2. 使用“总分 → 准入门槛 → 仓位预算”的顺序，明确三者关系。
3. 展示第 4 节全部评分表、硬性过滤、公式和边界值。
4. 增加三个示例：高分但硬性失败、70 分可试仓但不足一手、市场数据缺失时 90/90 且 10 分待判定。
5. 显示“规则最后核对日期”和数据口径：完整交易日、前复权、中证全指、至少 250 个有效交易日。
6. 参数必须来自共享策略配置或与实现同源的只读结构，禁止长期维护第二份手写常量。

### 5.3 验收标准

- [ ] 页面展示的每一分值、阈值和开闭区间与 0.4 实现一致。
- [ ] 页面明确写出评分不是上涨概率，高分不能越过硬性条件。
- [ ] 市场环境缺失时说明“待判定 10 分”，不写成市场环境 0 分。
- [ ] 策略版本切换后展示对应版本规则，不把 0.4 文案套到其他版本。
- [ ] 桌面端和手机端无需横向滚动即可阅读核心规则。

## 6. 功能 2：已持有仓位与止盈策略

### 6.1 交互模型

仓位方案中的每只股票增加状态切换：

| 状态 | 用途 | 主要输入 | 主要输出 |
|---|---|---|---|
| 计划买入 | 尚未成交 | 计划入场价、初始止损、已有市值 | 计划股数、计划亏损、仓位占比、限制原因 |
| 已持有 | 已真实成交 | 平均买入成本、实际股数、买入日期、初始止损 | 当前市值、浮动盈亏、收益率、R 倍数、当前保护止损、止盈/退出策略 |

用户从“计划买入”切换到“已持有”时，默认用计划入场价填充平均买入成本、用计划股数填充实际股数，但必须允许修改。计划股数为 0 时不自动切换，提示先填写真实成交数据。

### 6.2 输入与计算

已持有状态新增字段：

| 字段 | 必填 | 校验 |
|---|---|---|
| 平均买入成本 | 是 | 大于 0，支持最多 3 位小数 |
| 实际持有股数 | 是 | 大于 0 的整数；允许非 100 股整数倍，以兼容送转、零股尾差等实际持仓，并给予非阻断提示 |
| 买入日期 | 是 | 不晚于最新行情日期，用于计算持有交易日与时间退出 |
| 初始止损价 | 是 | 大于 0 且低于平均买入成本 |
| 盘中价格及均线 | 自动 | 打开页面时调用实时行情接口获取盘中价；均线和策略状态仅使用最新完整交易日日线。实时价失败时明确回退为最新收盘价 |

```text
初始每股风险 = 平均买入成本 - 初始止损价
1R价格 = 平均买入成本 + 初始每股风险
2R价格 = 平均买入成本 + 2 × 初始每股风险
浮动盈亏 = (当前价 - 平均买入成本) × 实际股数
收益率 = 当前价 / 平均买入成本 - 1
当前R倍数 = (当前价 - 平均买入成本) / 初始每股风险
```

费用首版不进入浮动盈亏，页面明确标注“未计交易费用”。

### 6.3 当前止盈/退出策略

止盈规则升级为与回测共享的移动止盈逻辑，并在页面使用“止盈/退出策略”作为准确名称。

| 优先级 | 触发条件 | 页面动作 |
|---:|---|---|
| 1 | 开盘价不高于当前有效止损 | 显示“跳空止损：按实际可成交价处理” |
| 2 | 盘中最低价触及当前有效止损 | 显示“触发初始止损”或“触发保护止损” |
| 3 | 收盘达到 1R | 当前有效止损至少上移至平均买入成本，阶段标记“保本保护” |
| 4 | MA20 不高于 MA60 | 收盘后生成退出信号，提示下一交易日开盘退出 |
| 5 | 收盘低于 MA60 的 98% | 收盘后生成退出信号，提示下一交易日开盘退出 |
| 6 | 连续 2 日收盘低于 MA20 | 收盘后生成退出信号，提示下一交易日开盘退出 |
| 7 | 收盘达到 2R 后，以 `max(上一止盈价, 1R价格, MA10 - 0.3 × ATR14)` 生成移动止盈价 | 新价格从下一交易日生效，只能上移；开盘或盘中触及后提示退出 |
| 8 | 持有满 10 个交易日、从未达到 1R，且收盘不高于成本 | 提示下一交易日开盘退出 |
| 9 | 持有满 40 个交易日 | 提示下一交易日开盘退出 |

页面必须区分“今日已经触发”和“尚未触发的下一条件”。收盘型信号不能显示成已按当日收盘成交。多个收盘条件同时成立时，按表中优先级展示首要退出原因。

### 6.4 盈利状态展示

当收益率大于 0 时，持仓卡重点展示：

- 当前盈利金额、收益率与 R 倍数；
- 当前阶段：`未到1R`、`保本保护（已到1R）`、`趋势止盈（已到2R）`；
- 当前有效止损和按该止损估算的剩余风险/已锁定收益；
- 下一观察项及距离，例如“距 2R 还差 ¥0.42”或“移动止盈价 ¥11.72，距盘中价 8.86%”；
- 已触发退出条件时，用明确的操作状态覆盖普通观察文案。

不能只按“当前价高于 1R/2R”推断历史上是否曾达到。系统需从买入日起回放日线，记录 `hitOneR`、`hitTwoR`、连续低于均线天数和当前有效止损。

### 6.5 异常与边界状态

- 买入日期之前缺少足够行情：显示可计算的成本盈亏，但暂停动态策略结论。
- 初始止损不低于成本：字段报错，不计算 R 和策略阶段。
- 行情落后：展示行情日期，不把旧价格描述为实时价格。
- 股票重复：计划买入项转为已持有时更新原项，不新增第二张卡。
- 用户修改成本、股数、买入日期或初始止损后，从买入日起重新回放状态。
- 每次修改后自动写入 PostgreSQL；刷新、关闭浏览器、重启设备或更换浏览器后，均从服务端恢复同一份模拟盘数据。
- 用户可主动“清空全部仓位数据”，清空前必须二次确认；确认后删除 PostgreSQL 中默认模拟盘的全部仓位项，但保留空的默认模拟盘。
- 多浏览器同时打开时，任一浏览器提交成功的修改都成为共享最新状态，其他浏览器应自动刷新或明确提示有新版本可载入。

### 6.6 验收标准

- [ ] 每只标的可在“计划买入”和“已持有”之间切换。
- [ ] 已持有状态可输入平均成本、实际股数、买入日期和初始止损。
- [ ] 盈亏、收益率、1R、2R 和当前 R 倍数计算正确。
- [ ] 达到 1R 后当前有效止损不低于成本，且不会因后续回落自动下调。
- [ ] 达到 2R 后生成 `MA10 - 0.3 × ATR14` 移动止盈价，至少锁定 1R，且止盈价只能上移。
- [ ] MA20/MA60、MA60 跌破、连续两日低于 MA20、10 日和 40 日规则与回测一致。
- [ ] 动态状态基于买入日起的完整路径，而非只看当前快照。
- [ ] 数据失败、非法成本和缺失历史不会生成误导性卖出结论。

## 7. 功能 3：从个股评分加入仓位方案

### 7.1 入口与按钮状态

个股评分页的风险回执区新增“加入仓位方案”按钮，与“查看近两年回测”并列。

| 当前评分状态 | 按钮行为 |
|---|---|
| 可执行候选 | 可点击，追加到仓位草案，并显示成功反馈 |
| T+1 开盘尚待确认 | 可加入，标记“待开盘确认”，仓位页继续使用计划入场价 |
| 人工问题未回答 | 禁用，并提示“完成事件风险与交易动机确认” |
| 等待转强、压力空间不足、硬性失败、分数不足、止损不可执行 | 禁用，并展示首要不可加入原因 |
| 风险额度不足或不足一手 | 允许加入；仓位容量由仓位页统一计算并解释为 0 股，避免把账户输入与股票资格混为一谈 |

### 7.2 传递字段

加入时至少携带：

| 字段 | 说明 |
|---|---|
| symbol、name、industry | 股票身份；行业未知时标记“未分类”并允许在仓位页补充 |
| score、technicalScore、strengthScore | 当前信号分数 |
| signalDate | 评分使用的完整交易日 |
| plannedEntryPrice、initialStopPrice | 计划入场和初始止损 |
| strategyId、strategyVersion、parameterVersion | 完整策略身份 |
| source | `individual-evaluation`，用于追踪入口 |
| confirmationState | 已确认或待 T+1 开盘确认 |

不得只传股票代码后在仓位页静默改用另一日或另一策略重新评分。行情更新需要重算时，必须提示用户并展示旧值与新值。

### 7.3 草案合并与导航

1. 使用 PostgreSQL 中唯一的默认模拟盘；“今日选股”和“个股评分”均追加到这份共享仓位方案。
2. 相同 `symbol + strategyId + strategyVersion + signalDate` 不重复添加，按钮变为“已在仓位方案”。
3. 同一股票但信号日或策略版本不同，提示用户确认“使用新信号更新原项”；确认后覆盖原项，不保留旧信号或审计记录，也不允许同一股票形成两个风险占用项。
4. 添加成功后不强制跳转，显示“查看仓位方案”；用户点击后进入 `/positions` 并看到刚添加的标的。
5. URL 只作为分享/降级入口，不作为唯一草案存储，避免从另一个页面加入时覆盖既有候选。

### 7.4 验收标准

- [ ] 个股评分页存在清晰的“加入仓位方案”入口。
- [ ] 不可执行状态不能被误加入，且用户能看到具体原因。
- [ ] T+1 待确认标的可以加入并带有待确认标记。
- [ ] 风险额度不足不阻止加入，仓位页可计算并解释 0 股结果。
- [ ] 添加后评分、入场价、止损价、信号日和策略版本不丢失。
- [ ] 重复点击不会创建重复项，也不会覆盖其他候选。
- [ ] 从今日选股和个股评分加入的标的出现在同一草案中。

## 8. 页面信息架构调整

| 页面 | 调整 |
|---|---|
| 个股评分 `/evaluate` | 增加“加入仓位方案”和“查看完整评分规则”；展示加入资格及首要阻塞原因。 |
| 评分规则 `/methodology/stock-score` | 新增只读规则说明页，按策略版本展示。 |
| 仓位方案 `/positions` | 每只股票增加计划/已持有状态；已持有卡展示成本盈亏、R 阶段、有效止损和止盈/退出策略。 |
| 今日选股 `/` | 保持现有加入入口，但改为写入 PostgreSQL 中的默认共享模拟盘。 |

仓位页顶部摘要需同时区分“计划市值/新增计划风险”和“已持有市值/当前保护风险”，不能把已成交仓位重复计为待买入资金。

## 9. 数据与接口建议

### 9.1 PostgreSQL 共享模拟盘

系统在 PostgreSQL 中初始化且始终保留一条默认模拟盘记录，固定键建议为 `default-simulation`。所有浏览器不带用户身份直接读取和修改这条记录，不创建浏览器级、设备级或用户级副本。

建议数据模型：

| 表 | 关键字段 | 说明 |
|---|---|---|
| `position_plans` | `id`、`plan_key`、`account_equity`、`current_open_risk`、`three_consecutive_stops`、`revision`、`updated_at` | `plan_key` 固定且唯一为 `default-simulation`；保存账户级输入和当前数据版本 |
| `position_plan_items` | `id`、`plan_id`、`symbol`、`priority`、`position_state`、策略身份、评分、信号日、计划入场价、初始止损、买入成本、实际股数、买入日期、确认状态、`updated_at` | 保存计划买入和已持有项；同一默认模拟盘内股票代码唯一 |

数据库要求：

- 数值字段使用定点数类型，不使用浮点数保存价格和金额；
- 新增、更新、删除、排序和状态切换均立即持久化，不设置自动过期；
- 新信号更新同一股票时直接覆盖当前记录，不新增历史表，不保留旧信号审计；
- 清空操作在单个事务中删除全部仓位项并重置账户级输入；
- 数据库迁移负责创建默认模拟盘；记录意外缺失时，接口应幂等重建空模拟盘。

### 9.2 多浏览器一致性

默认模拟盘使用单调递增的 `revision`。每次写入成功后递增版本并返回完整最新状态。

- 页面打开时从 PostgreSQL 读取，不读取浏览器缓存作为权威数据；
- 页面保持打开时，建议每 5 秒轮询最新 `revision`；条件允许时可改用 Server-Sent Events；
- 本地没有未提交编辑时，发现新版本自动刷新页面状态；
- 本地正在编辑时，发现新版本提示“模拟盘已在其他页面更新”，用户载入最新版本后再继续编辑；
- 写接口提交客户端已知 `revision`。版本过期时返回 `409 CONFLICT` 和最新状态，禁止静默覆盖其他浏览器刚完成的修改；
- 数据库写入和 `revision` 更新必须在同一事务内完成。

### 9.3 行情与持仓状态

新增或扩展只读接口，输入股票、买入日期、成本和初始止损，返回：

- 打开页面时取得的盘中价、报价时间和来源，以及最新完整交易日收盘价；
- 买入日至今的 MA10、MA20、MA60 和日线触发路径；
- 是否曾达到 1R/2R、连续低于 MA20 天数、持有交易日数；
- 当前有效止损、移动止盈价、当前阶段、已触发退出原因和下一观察项；
- 策略身份和计算时间。

持仓状态计算必须与回测共享同一退出状态机，不能在组件中复制一套近似判断。

### 9.4 兼容现有仓位预览

`POST /api/positions/preview` 继续负责计划买入的风险仓位。已持有项不再生成计划股数，但其当前风险和行业/单股市值应参与后续计划项的组合容量计算。

新增共享模拟盘接口：

| 接口 | 用途 |
|---|---|
| `GET /api/position-plan` | 返回默认模拟盘、全部仓位项和当前 `revision` |
| `PATCH /api/position-plan` | 更新账户净值、当前风险和连续止损状态 |
| `POST /api/position-plan/items` | 新增股票；重复代码返回当前记录或按确认后的新信号更新 |
| `PATCH /api/position-plan/items/{id}` | 更新计划/已持有状态、成本、股数、日期、止损或优先级 |
| `DELETE /api/position-plan/items/{id}` | 删除单个仓位项 |
| `DELETE /api/position-plan/items` | 二次确认后清空默认模拟盘 |

所有写接口携带客户端已知 `revision`，冲突时返回 `409` 和服务端最新状态。

## 10. 埋点与效果指标

首版可记录匿名页面事件，不记录账户净值、成本、股数等敏感数值。

| 指标 | 定义 |
|---|---|
| 规则页到达率 | 查看个股评分的会话中打开评分规则页的比例 |
| 评分加入率 | 可执行个股评分中点击加入仓位方案的比例 |
| 加入成功率 | 点击加入后成功写入 PostgreSQL 默认模拟盘的比例 |
| 已持有使用率 | 仓位草案中切换为已持有的标的比例 |
| 状态计算成功率 | 已持有项成功获得动态退出状态的比例 |

## 11. 测试要求

### 11.1 评分回归

- 覆盖所有评分区间边界，尤其 2%、3%、5%、6%、8%、10%、12%、20%。
- 固定同一组日线，验证个股页、扫描页、规则示例和回测的分数一致。
- 验证市场环境失败时为 90 分已判定、10 分待判定。
- 验证高分不能越过硬性过滤和人工否决。

### 11.2 持仓状态机

- 1R 当日收盘后保护止损上移到成本。
- 达到 1R 后回落，保护止损不下调。
- 达到 2R 后移动止盈价至少为 1R，并随 `MA10 - 0.3 × ATR14` 单向上移；新止盈价下一交易日生效。
- 同时触发多个退出条件时使用规定优先级。
- 跳空越过止损使用开盘触发文案，不假设按止损价成交。
- 收盘型信号不发生未来数据泄漏。

### 11.3 草案与串联

- 从两个入口连续加入多只股票，不覆盖已有项。
- 重复股票、不同信号日、不同策略版本均按规则处理。
- 刷新、关闭浏览器、重启设备或更换浏览器后读取到相同的 PostgreSQL 数据。
- 非 100 股整数倍可以保存并正常计算，只显示非阻断提示。
- 两个浏览器先后修改时，后打开或刷新的一方能看到最新状态。
- 两个浏览器基于相同旧版本同时写入时，其中一个成功，另一个收到冲突和最新数据，不静默覆盖。
- 数据库迁移、默认模拟盘幂等初始化和异常降级可用。
- 手机端按钮、字段和策略状态无重叠，长股票名和长退出原因可换行。

## 12. 实施顺序

### Phase 0：统一规则基座

- 统一单股评分与全市场扫描的止损价格精度。
- 把退出判断抽成可由回测和持仓页面共同调用的状态机。
- 为 0.4 评分和退出规则补齐边界回归测试。

### Phase 1：评分说明与加入链路

- 上线评分规则页。
- 建立 PostgreSQL 默认模拟盘、持久化接口、版本冲突控制和跨浏览器刷新机制。
- 在个股评分页接入加入按钮、资格判断、去重和导航。

### Phase 2：已持有与止盈策略

- 增加已持有输入、盈亏与 R 计算。
- 接入历史路径回放、有效止损和退出提示。
- 将已持有风险纳入新增计划仓位的组合容量计算。

## 13. 发布门槛

- [ ] 评分规则页已由代码实现逐项核对，未引用未上线的 0.5 草案规则。
- [ ] 同股、同日、同策略在扫描、个股评分、仓位和回测中的关键结果一致。
- [ ] 已持有页面的退出提示与 0.4 回测状态机逐日对照一致。
- [ ] 所有账户和成本输入默认不发送到埋点。
- [ ] 页面明确展示行情日期、策略版本和“不自动交易”声明。
- [ ] 核心流程完成桌面端与手机端验收。

## 14. 已确认产品决策

1. 仓位数据保存到 PostgreSQL，首版不处理登录、身份或权限。所有浏览器和设备读取、修改同一份 `default-simulation` 模拟盘数据。
2. 实际持有股数允许非 100 股整数倍，用于兼容送转和零股尾差；页面仅提示，不阻断保存和计算。
3. 同一股票的新信号更新原项时，不保留旧信号或审计记录。
